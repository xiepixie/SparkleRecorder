# SparkleRecorder Documentation Status

Updated: 2026-07-06

This index records which docs are active plans, which are implementation references, and which are archival. "Done" means the capability is present in the current worktree or is already covered by tests; it does not mean the whole product migration is finished.

## Current Completion Snapshot

| Area | Status | Source of Truth |
| --- | --- | --- |
| Swift 6 baseline | Done in current worktree | `Package.swift` and `DomainSendableTests` |
| Swift Testing baseline | Done | `Tests/SparkleRecorderTests` uses `Testing`, `@Test`, `#expect` |
| Recording boundary split | Mostly done | `RecordingEngineClient`, `LiveRecordingEngineClient`, `RawInputEvent`, `RecordingEventPipeline`, `RecordingSessionProcessor` |
| Event tap start failure feedback | Done in current worktree | `EventTapThread.startAndWait`, `Recorder.startRecording() -> Bool` |
| Recording high-frequency buffering | Done in current worktree | `RecordingEventBuffer` and bounded `AsyncStream` policy |
| Semantic recording video/AI roadmap | Active plan | `docs/semantic-recording-ai/`; defines the future dual-track macro recording model where playable `RecordedEvent` output is paired with video/keyframes, OCR/visual/AX/window evidence, AI-safe `events.jsonl`, visual asset extraction, CLI-first AI queries, recording cleanup suggestions, and app knowledge composition |
| Playback ordinary step executor | Mostly done | `PlaybackStepExecutor` and tests |
| Playback failure evidence value model | Mostly done | `PlaybackFailureEvidence`, `PlaybackFailureEvidenceBuilder`, `PlaybackEvidenceClient` |
| Playback run lifecycle state machine | First pass done | `PlaybackRunStateMachine` and tests |
| Playback live run engine | First pass done | `PlaybackRunEngine` and `PlaybackRunEngineTests` |
| Playback live locator/OCR step client | First pass done | `LivePlaybackRunStepClient`, `PlaybackLocatorCache`, `PlaybackLocatorCacheTests` |
| Playback synchronous run engine | First pass done | `PlaybackSynchronousRunEngine`, `LivePlaybackSynchronousRunStepClient`, `PlaybackSynchronousRunEngineTests` |
| SwiftUI selection recalculation control | Done in current worktree | `ActionGroupSelectionSnapshot`, `ActionGroupProjectionTests` |
| AutomationEngine core contract | Done in current worktree | `AutomationContract.swift`, `AutomationContractTests` |
| Workflow AI macro catalog, workflow read/status/export, runtime run/cancel/runs, draft validation, draft simulation, draft editing/patch, import dry-run, and confirmed import | First pass done | `AutomationWorkflowDraft.swift`, `AutomationWorkflowDraftEditor.swift`, `AutomationWorkflowDraftPatch.swift`, `AutomationWorkflowDraftExport.swift`, `AutomationWorkflowDraftSimulation.swift`, `AutomationWorkflowDraftImport.swift`, `AutomationCLIResult.swift`, `AutomationWorkflowDraftTests`, `AutomationWorkflowRuntimeCLITests`, `workflow macros`, `workflow list/show/status/export`, `workflow run/cancel/runs`, `workflow draft init/inspect/normalize/patch/task add/task set/task remove/schedule set/condition set/dependency add/dependency set/dependency remove`, `workflow draft validate`, `workflow draft simulate`, `workflow import --dry-run`, and `workflow import --confirm` CLI |
| Workflow runtime App-host handoff mailbox and receipt status | First pass done | `AutomationRuntimeHandoff`, `AutomationRuntimeHandoffClient`, `AutomationRuntimeHandoffStore`, `AutomationRuntimeHandoffReceipt`, `AutomationRuntimeHandoffStatusPayload.runs`, `AutomationRuntimeHandoffStatusPayload.workflowStatus`, CLI `workflow run/cancel --handoff app`, CLI `workflow handoff status`, `LiveAutomationRuntimeHost` mailbox polling/receipt writing, `AutomationOwnerBClientTests`, `AutomationWorkflowRuntimeCLITests`, CLI smoke; status now reports command delivery plus repository-backed run snapshots/workflow status when receipts include run IDs |
| Workflow reducer and state machine | Done in current worktree | `AutomationReducer.swift`, `AutomationReducerTests`, `AutomationViewProjection` |
| Workflow next scheduled occurrence projection | First pass done | `AutomationScheduleOccurrence.swift`, `AutomationViewProjection`, `AutomationScheduleOccurrenceTests`, `AutomationViewProjectionTests` |
| Workflow runtime feedback projection | First pass done | `AutomationTimeoutCountdownProjection`, `AutomationRetryAttemptSummary`, `AutomationTaskNodeProjection`, `AutomationResourceTimelineItem`, `AutomationViewProjectionTests` |
| Workflow runtime branch decision evidence | First pass done | `AutomationBranchDecisionEvidence`, `AutomationTaskRun.branchEvidence`, `AutomationReducer.completeRun`, `AutomationDependencyEdgeProjection.branchDecision`, `AutomationViewProjection`, `AutomationFlowGraphEdgeLabelView`, `AutomationFlowGraphBranchGuideView`, `AutomationTaskBranchDecisionSummaryView`, `AutomationTaskBranchPanelView`, `AutomationTaskRunBranchContextView`, `AutomationReducerTests`, `AutomationOwnerBClientTests`, `AutomationViewProjectionTests`; reducer writes durable triggered/skipped/disabled branch decisions onto the completed source run, repository JSON persists them with run history, and projection/UI prefer durable evidence with projection fallback for older runs |
| Workflow Task Run per-run-aware evidence viewer | First pass done | `RunEvidenceManifest`, `MacroRepository.saveRunEvidence`, failed playback `AutomationTaskRun.evidenceID` binding, `AutomationTaskRunEvidencePresenter`, `AutomationTaskRunEvidenceBindingView`, `AutomationTaskRunEvidenceDiagnosticsView`, `AutomationTaskRunEvidenceManifestView`, `AutomationTaskRunEvidenceSectionView`, `AutomationTaskRunEvidenceScreenshotPreviewView`, `AutomationTaskRunDetailView`, `RunEvidenceTests`, `AutomationReducerTests`; UI prefers `runs/<evidenceID>/manifest.json` / `report.json`, shows binding status, diagnostic focus/preview/next-check summary, and manifest details when present, falls back only to matching legacy latest evidence, and keeps report visible if screenshot preview data cannot be read |
| Workflow repeating scheduled run generation | First pass done | `AutomationScheduleOccurrence.nextDueOccurrence`, `AutomationReducer.clockTick`, `AutomationReducerTests`, `AutomationScheduleOccurrenceTests` |
| Workflow resource waiting queue/resume | First pass done | `AutomationReducer.resourceLeaseDenied`, `refreshWaitingResourceRuns`, `AutomationReducerTests`, `AutomationOwnerBClientTests` |
| Workflow resource max-wait policy | First pass done | `AutomationResourceRequirement.maxWaitDuration`, reducer `expireResourceWaits`, draft `maxResourceWaitSeconds`, CLI `--max-resource-wait`, `AutomationReducerTests`, `AutomationWorkflowDraftTests`, `AutomationViewProjectionTests` |
| Workflow resource waiting reason projection | First pass done | `AutomationResourceWaitingProjection`, `AutomationResourceBlockerProjection`, `AutomationTaskNodeProjection.resourceWaiting`, `AutomationResourceTimelineItem.resourceWaiting`, `AutomationViewProjectionTests`; projection includes wait deadline/remaining/fraction when max wait is configured |
| Workflow timeout watchdog | First pass done | `AutomationReducer.clockTick`, `expireTimedOutRuns`, `AutomationReducerTests` |
| Workflow retry policy | First pass done | `AutomationReducer.completeRun`, `AutomationRetryPolicy`, `AutomationReducerTests` |
| Workflow join policy | First pass done | `AutomationJoinPolicy`, `AutomationTask.joinPolicy`, `AutomationReducer.dependencyResolution`, `AutomationTaskNodeProjection.joinPolicy`, `AutomationWorkflowDraftTask.joinPolicy`, `AutomationReducerTests`, `AutomationWorkflowDraftTests`, `AutomationViewProjectionTests` |
| Workflow visual condition core spec | First pass done | `AutomationVisualCondition`, `AutomationConditionKind.visual`, `AutomationWorkflowDraftCondition.imageRef/baselineRef/pixel/colorHex/threshold`, `AutomationConditionEvaluatorClient.contextual`, `AutomationContractTests`, `AutomationWorkflowDraftTests`, `AutomationOwnerBClientTests` |
| Workflow visual condition progress projection | First pass done | `AutomationConditionProgressProjection`, `AutomationTaskNodeProjection.conditionProgress`, `AutomationResourceTimelineItem.conditionProgress`, `AutomationProjectionFixture.ownerCFixture`, `AutomationViewProjectionTests` |
| Workflow live visual condition evaluator | First pass done | `AutomationVisualConditionEvaluatorClient.live`, `LiveAutomationConditionEvaluatorClient`, `AutomationVisualCondition.searchRegionResolution`, `AutomationContractTests.visualConditionSearchRegionResolvesCoordinateSpaces`, Swift 6 build |
| Workflow condition diagnostics evidence | First pass done | `AutomationConditionEvaluationResult`, `AutomationConditionEvaluationEvidence`, `AutomationConditionDiagnosticArtifact`, `AutomationTaskRun.conditionEvidence`, `AutomationConditionEvidenceArtifactWriter`, `AutomationConditionEvidenceArtifactPresenter`, `AutomationEffectRunner`, `LiveAutomationConditionEvaluatorClient`, `AutomationVisualConditionEvaluatorClient`, `AutomationTaskRunConditionEvidenceView`, `AutomationTaskRunEvidenceReadinessView`, `AutomationContractTests`, `AutomationReducerTests`, `AutomationOwnerBClientTests`, `AutomationViewProjectionTests`; OCR/visual evaluators now return outcome plus durable diagnostics, context-only conditions (`previousOutcome`, external signal, manual approval) now return explanatory evidence, reducer persists diagnostics on the terminal condition run, repository JSON round-trips it in run history, graph/timeline evidence indicators include condition diagnostics, live evaluators persist last-sample plus watched-region image artifact references under App Support, captured visual samples are retained even when bitmap decode later fails, failure/rejected terminal paths still return explanatory payloads when possible, and UI artifact preview/open/reveal must go through the app-edge presenter with safe relative-path resolution plus action feedback |
| Workflow visual asset/region registry for AI drafts | First pass done | `AutomationWorkflowDraftVisualAssets`, `AutomationWorkflowDraftVisualRegion`, `AutomationWorkflowDraftVisualImageAsset`, `AutomationWorkflowDraftImporter`, `AutomationWorkflowDraftExporter`, `AutomationWorkflowDraftTests` |
| Workflow visual assets persistence and package provider binding | First pass done | `AutomationWorkflow.visualAssets`, `AutomationWorkflowDraftImporter`, `AutomationWorkflowDraftExporter`, `AutomationWorkflowPackage`, `AutomationVisualAssetWorkflowPackage`, `AutomationVisualAssetImageProviders.workflowPackages`, `LiveAutomationRuntimeHost.visualAssetPackages`, `AutomationContractTests`, `AutomationWorkflowDraftTests`, `AutomationOwnerBClientTests`, Swift 6 build |
| Workflow visual asset package-root retention | First pass done | `AutomationVisualAssetPackageRoot`, `AutomationVisualAssetPackageRootClient`, `AutomationVisualAssetPackageRootStore`, CLI `workflow import --confirm --visual-assets-root`, `AutomationWorkflowPackagePresenter`, `AutomationMainContentView`, `LiveAutomationRuntimeHost`, `AutomationOwnerBClientTests`, CLI smoke, Swift 6 build |
| Workflow AI Draft Preview baseline capture | First pass done | `AutomationWorkflowDraftBaselineCapturePresenter`, `AutomationWorkflowDraftBaselineCaptureResult`, `AutomationWorkflowDraftVisualAssetEditorView`, `AutomationOCRRegionPickerOverlay`, Swift 6 build, full Swift Testing |
| Workflow AI Draft Preview external visual asset import/copy | First pass done | `AutomationWorkflowDraftVisualAssetImportPresenter`, `AutomationWorkflowDraftVisualAssetActionButtonsView`, `AutomationWorkflowDraftVisualAssetEditorView`, Swift 6 build, full Swift Testing |
| Workflow Pro visual/UX product contract | Active acceptance gate | `docs/workflow-page-productization/06-workflow-visual-ux-contract.md`, `workstreams/product-ui-ux.md`, `acceptance-checklist.md`; current hard gates are Pro native restraint, "UI should disappear" visual hierarchy, `.controlSurface` policy, source-bin drag/drop orchestration, visible dependency graph levels, and projection-driven runtime feedback |
| Workflow product evidence audit gate | First pass done | `AutomationProductEvidenceAudit`, `AutomationProductEvidenceAuditTests`, CLI `workflow product-evidence audit`, CLI `workflow product-evidence capture-plan`, CLI `workflow product-evidence prepare-live-capture`, CLI `workflow product-evidence complete-sidecar`, and CLI `workflow product-evidence sidecar-template` report fixture/live evidence files under `docs/workflow-page-productization/product-evidence/`; `capture-plan` lists missing live gates, accepted filenames, sidecar commands, missing/invalid labels and undersized live clips, `prepare-live-capture` writes placeholder sidecar drafts without satisfying strict audit, and `complete-sidecar` fills reviewed sidecar fields while validating clip filenames. Live clips may be `.mov` or `.mp4`; strict mode validates required live sidecar labels, rejects placeholders, rejects fixture/mock/synthetic evidence sources, requires `Clip file:` to name one accepted candidate matching the satisfying file group, and rejects zero-byte or size-unknown clips before an item is satisfied. `--require-live` exits non-zero while S0 live-product artifacts are missing. Current smoke: 9/13 required present; five live sidecar drafts exist, but all four S0 live gates still lack real clips and completed sidecar fields. |
| Semantic recording bundle v0 contract | First pass done | `SemanticRecordingBundle.swift`, `SemanticRecordingFixture.swift`, `SemanticRecordingBundleTests.swift`, `docs/semantic-recording-ai/workstreams/s1-contract-core.md`; covers schema versioning, safe relative artifact refs, video/frame/event alignment helpers, AI-safe event boundary, suppression records, S0 source/runtime preview comparison refs, and deterministic bundle/query/suggestion fixtures for S2/S3/S4. |
| Semantic recording S2 capture session | First pass done; live evidence open | `SemanticRecordingCapture.swift`, `SemanticRecordingCaptureTests.swift`, `SemanticRecordingPreflight.swift`, `SemanticRecordingPreflightPresentation.swift`, `SemanticRecordingPreflightTests.swift`, `SemanticRecordingPreflightPresentationTests.swift`, `SemanticRecordingSuppression.swift`, `SemanticRecordingSuppressionTests.swift`, `ScreenCaptureKitSemanticCapture.swift`, `VisionRecordingIndexer.swift`, `SemanticRecordingFrameObservationIndexer.swift`, `RecordingBundleStore.swift`, `LiveSemanticRecordingPreflight.swift`, `docs/semantic-recording-ai/workstreams/s2-app-capture-visual-index.md`; fake clients prove movie segment + event-aligned keyframes + OCR/window/AX observation attachment + keyframe-only mode, preflight tests prove blocking/degraded permission semantics, preflight presentation tests prove ready/blocked/degraded app-shell guidance, suppression tests prove Secure Input/password/excluded-target/private/oversized record production, app-edge ScreenCaptureKit/Vision/window/AX/store/PermissionCenter skeletons compile, and `semantic-recording debug-smoke --preflight-only` uses a CLI-safe permission snapshot before any ScreenCaptureKit work. Ordinary macro recording lifecycle wiring, SwiftUI preflight UX wiring, live `.mov` product evidence, live suppression detection and retention/deletion UX remain open. |
| Semantic recording preview-ref rendering | Fixture proof done; user-facing UI still open | S1 accepts source/runtime/comparison refs, and S0 added `template-baseline-preview-refs.png` / `.md` proving Source Reference, Runtime Sample and Decision render together from `SemanticRecordingFixture.checkoutBundle`. S3 still needs to render the same evidence inside real Macro Review or Run Detail without SwiftUI constructing raw paths. |
| ResourceArbiter and panic release | Done in current worktree | `AutomationResourceArbiter.swift`, `AutomationOwnerBClientTests` |
| SchedulerClient and timed starts | Done in current worktree | `AutomationSchedulerClient.swift`, `AutomationRuntimeSession.swift`, `AutomationOwnerBClientTests` |
| FlowGraph and Resource Timeline UI | First pass done | `AutomationMainView`, FlowGraph projection views, Resource Timeline views, `AutomationViewProjectionTests` |

## Document Roles

| Document | Role | Status |
| --- | --- | --- |
| `ArchitectureAndTestingModernizationPlan2026.md` | Master architecture and testing direction | Active plan. Use with this status index because early "current state" notes are an audit snapshot. |
| `SparkleRecorderArchitecture.md` | Product architecture snapshot | Active reference. Needs refresh after AutomationEngine lands. |
| `RecordingEngineRefactoringPlan.md` | Recording migration plan | Partially implemented. Keep remaining actor/performance work here. |
| `WindowBoundAutomationPlan.md` | Window/OCR/semantic automation architecture | Partially implemented reference. Automation condition routing now belongs to `automation-engine/`. |
| `VisionArchitecturePlan.md` | OCR and visual targeting rules | Active reference. State tree work has moved to AutomationEngine planning. |
| `FormatEvolutionPlan.md` | `.tinyrec`, `.sparkrec`, `.sparkflow` evolution | Deferred reference. First workflow persistence should use `automations.json`. |
| `CodebaseTargetModifications.md` | File-level target-change notes | Archive. Do not treat snippets as exact current source. |
| `MacroEditorUserGuide.zh-Hans.md` | User-facing editor guide | Active user doc. Does not cover future FlowGraph UI yet. |
| `RepositorySubmissionPlan.md` | Repository publication checklist | Archive/reference. Re-check before push. |
| `automation-engine/` | Automation scheduling, dependency, reducer, resource, UI and testing plan | Active workstream. Phase 0 core contract is done; three-owner planning lives in `automation-engine/workstreams/`. |
| `workflow-page-productization/` | Next-stage Workflow page, CLI-first AI interface, Pro macOS visual/UX contract, two-owner productization plan, product-evidence template, and current architecture pause snapshot | Active plan. Use this for the next phase where Engine/Runtime contracts feed a redesigned usable Workflow page. The visual contract is now explicit: Pro native macOS surface, restrained controls, no ordinary `.controlSurface` / high-saturation full-surface controls, Macro Library as a source bin, drag-and-drop orchestration, visible dependency graph / If-Then branches, and live execution feedback. `08-current-architecture-and-future.md` is the current pause snapshot for architecture, backend logic, file structure, and future design. `product-evidence/README.md` defines the idle, drag/link, task-reorder, running, and future drill-in artifact requirements; UI review must include those states, not only a static restyle. |
| `semantic-recording-ai/` | Future semantic macro recording, video evidence, visual indexing, CLI-first AI query/draft contract, macro cleanup suggestions, app knowledge organization, and Workflow evidence bridge | Active plan plus S1/S2 first pass. Use this when extending recording beyond low-level input events or when deciding how current Workflow evidence gaps feed the next product direction. It treats video/keyframes as a semantic evidence layer, keeps `RecordedEvent` as execution truth, routes ScreenCaptureKit/Vision/AX/file IO through app-edge adapters, defers MCP until CLI/shared service contracts stabilize, uses `05-workflow-continuation-and-direction-review.md` for route judgment, `06-current-work-and-next-tasks.md` as the current execution ledger, `07-apple-api-implementation-path.md` for the macOS 15+ `SCRecordingOutput` video path, `08-parallel-workstreams.md` for S0/S1/S2/S3/S4 owner boundaries, `09-template-baseline-preview-refs.md` for the accepted S0 -> S1 preview-ref contract, `10-next-stage-reality-check.md` for user-logic and overdesign checks, and `workstreams/s0-workflow-evidence.md` / `workstreams/s1-contract-core.md` / `workstreams/s2-app-capture-visual-index.md` for active coordination. |

## Next Required Sequence

1. Freeze the Workflow page product and visual/UX contracts in `workflow-page-productization/`, including the Pro macOS restraint rule: ordinary controls avoid high-saturation full-width backgrounds and unnecessary `.controlSurface` emphasis; user data and runtime state outrank ordinary commands; Macro Library acts as a source bin for drag/drop orchestration; visual dependency graph, If/Then branch readability, and live execution feedback are first-class product requirements. Owner 2 UI changes need idle, drag/link, and running-state evidence in `workflow-page-productization/product-evidence/` plus an exception log for any retained strong control background.
2. Continue backend ready-for-ui notices for resource queue policy and daemon/background runtime handoff. CLI-first AI macro catalog, workflow read/status/export, runtime run/cancel/runs first pass, App-host handoff mailbox + receipt/status/result readback first pass, next scheduled occurrence projection first pass, runtime feedback projection first pass, repeating scheduled run generation first pass, resource waiting queue/resume first pass, resource waiting reason projection first pass, timeout watchdog first pass, retry policy first pass, join policy first pass, visual condition core spec first pass, visual condition progress projection first pass, live visual evaluator first pass, draft visual asset/region registry first pass, workflow visualAssets persistence first pass, workflow-scoped package provider binding first pass, package-root retention first pass, AI Draft Preview package-local image/baseline registration first pass, AI Draft Preview baseline capture first pass, AI Draft Preview external visual asset import/copy first pass, draft editing/patch first pass, draft validation, draft simulation, import dry-run, and confirmed import have a first pass. Broader managed visual asset storage/migration policy, product evidence, daemon/background runtime sessions, and push-style live handoff result/progress streaming remain separate productization work.
3. Use `semantic-recording-ai/05-workflow-continuation-and-direction-review.md` and `semantic-recording-ai/06-current-work-and-next-tasks.md` to close the current Workflow evidence gap before opening a broad AI roadmap: live visual diagnostics Open/Reveal recording, real branch evidence consistency recording, template/baseline preview refs, and real drag/reorder clips should become product evidence, not just fixture proof.
4. Continue semantic recording from the S1 contract first pass: S2 should prove `.mov` plus event-aligned keyframes against `SemanticRecordingBundle`; S3 should open fixture Review UI from the same bundle shape; S4 should start fixture-backed `recording show` / `recording frames`; retention/deletion policy and frame-derived `AutomationWorkflowDraftVisualAssets` copy semantics remain open before frame-to-condition ships.
5. Rebuild the Workflow UI from fixtures before live wiring.
6. Evaluate product-level scheduler launch integration such as `NSBackgroundActivityScheduler` or login item.
7. Polish release-facing AutomationEngine documentation after product flows stabilize.
8. Refresh product-facing docs after AutomationEngine UI stabilizes.
