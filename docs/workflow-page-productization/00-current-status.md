# Current Status

更新时间：2026-07-06

本文只记录下一阶段 Workflow 页面的真实状态。已经有代码和测试的底层能力可以称为 first pass，但用户不可用的页面不能称为完成。

## 已经可以作为输入的底座

| Area | Status | Notes |
| --- | --- | --- |
| 静态工作流模型 | First pass available | `AutomationWorkflow` 表示一个工作流图，和 `SavedMacro` 分离。 |
| 静态任务节点 | First pass available | `AutomationTask` 可以是 macro、condition、delay、notification。 |
| 运行实例 | First pass available | `AutomationTaskRun` 有独立 `runID`、`executionID`、时间、outcome、evidence metadata。 |
| 依赖边 | First pass available | `AutomationDependency` 支持 success/failure/timeout/cancel/condition 等触发。 |
| reducer/effect 边界 | First pass available | 所有运行刺激通过 `AutomationAction`，真实副作用通过 `AutomationEffect` 交给 runtime/adapter 边界。 |
| adapter/runtime 边界 | First pass available | Player、scheduler、condition、repository 已有 mockable client 和 runtime session。 |
| persistence | First pass available | `automations.json` 保存 workflows/run history，`.sparkrec_workflow` 保存静态 workflow package。 |
| projection/UI bridge | First pass available | UI 能读取 projection 并提交部分 reducer action。 |

## 当前不能按产品完成验收的缺口

| Gap | Owner | Why It Blocks Product UX |
| --- | --- | --- |
| Workflow 页面交互不可用 | UI | 用户无法稳定拖拽、拉线、编辑和理解运行状态。 |
| Workflow 视觉语言还需收敛 | UI | 页面必须符合 Pro macOS 原生生产力工具定位；普通控件不能依赖 `.controlSurface`、全宽高饱和底色或 Web dashboard 式组件来表达重要性。 |
| 重复调度后台产品化仍不完整 | Engine | Reducer `clockTick` 已能为 repeating schedule 创建下一个已到期且未表示的独立 run first pass；App-host mailbox handoff 已能把 CLI run/cancel 请求交给正在运行的 App host；仍缺 OS 级唤醒、长期 daemon/background session handoff、未来多 occurrence timeline 预览和资源等待联动。 |
| 资源等待策略仍需产品化 | Engine/UI | Reducer 已有 first pass：busy/denied 资源会保持 `waitingForResource`，release/tick 会重试等待 run；UI projection 现在把 resource wait 显示为 `.waiting`，并通过 `resourceWaiting` 暴露等待原因、资源列表、优先级、等待时长和 visible active-lease blocker；OwnerC fixture 已包含 foreground-input wait 样本；仍缺真正的优先级调度、最大等待策略、lease-expiration watchdog UI 和跨进程资源协调。 |
| retry/timeout 产品化仍不完整 | Engine/UI | Timeout watchdog 和 retry reducer first pass 已能让 queued/running task 超时为 `.timedOut`、按 `retryPolicy` 创建下一 attempt、保留 run history，并在最终失败/超时后触发分支；`timeoutCountdown` 和 `retryAttemptSummary` projection first pass 已被 UI 在 Graph/Timeline/Task Inspector 中消费为 compact countdown/progress/retry detail；Task Run History 现在有 selectable detail first pass，展示 run/execution/attempt、evidence ID、retry exhausted 和 cancel/next-check 文案；Task Run Evidence viewer first pass 会优先读取 macro package 内 `runs/<evidenceID>/manifest.json` / `report.json`，显示 binding status、diagnostic focus/preview/next-check 摘要、manifest created/run/macro/file 绑定信息，找不到时才降级到 matching legacy latest evidence，并且截图预览不可读时仍保留 report payload；Reveal Report / Open Screenshot 按钮现在贴近 verified binding，并把成功/失败反馈回写到 Run Detail；Run Detail 现在可读取 durable `AutomationTaskRun.conditionEvidence` 和 durable branch evidence / projection fallback；condition diagnostics 可带 live last-sample / watched-region artifact refs 并在 Run Detail 中预览 image artifact，Open/Reveal artifact action 也会回写成功/失败反馈；idle/running/connector drag-link/task-reorder/failed-run/preview-unavailable/branch/visual diagnostics fixture product evidence 已补，仍缺完整真实 drag/reorder 录屏、Open/Reveal 交互录屏和 live capture 录屏。 |
| 条件合流产品化仍需 UI 表达 | Engine/UI | Engine first pass 已提供 `AutomationTask.joinPolicy`、reducer `all` / `any` / `firstMatched` 语义、draft/CLI import/export/edit 支持和 node projection 文案；UI first pass 已让 projection 暴露 incoming dependency count，FlowGraph 对多入边/非默认合流显示克制 badge，Task Inspector 可编辑并解释 join policy，中心图现在有 then/else/timeout branch guide first pass，选中条件 task 时右侧已有 If/Then/Else branch panel first pass，可查看/选择/删除 Then、Else、Timeout、Always、Cancel 分支；runtime branch decision evidence first pass 会在 terminal source run 写入 `AutomationTaskRun.branchEvidence`，随 run history 持久化，并让 dependency edge projection/Inspector/Run Detail 优先读取 durable payload；branch evidence fixture drill-in 已补，仍缺 drag/link 交互验收录屏和更完整 polish。 |
| 视觉条件产品化仍不完整 | Engine/UI | Engine first pass 已提供 `AutomationConditionKind.visual`、`AutomationVisualCondition`、draft import/export/editor/patch/CLI 字段、node/timeline `conditionProgress` projection，以及 app-edge live visual evaluator：默认 live path 可截图评估 pixel/color，并通过 injected image/baseline provider 支持 image appeared/disappeared 与 region changed；`sparkle.workflow.draft.v1` 现在也有 `visualAssets` registry first pass，可把 `regionRef` materialize 为 concrete search region，并让 export 把 OCR/visual concrete regions 写回 package-level region registry；workflow visualAssets persistence + workflow-scoped package provider binding first pass 已能让 internal workflow/package 保留 `visualAssets`，并从 package-local `visualAssets.images/baselines` 构造 request-aware image/baseline providers；package-root retention first pass 已能把 confirmed import、AI Draft Preview import 和 workflow package import 的本地 root 记录到 repository 旁的 manifest，runtime host 会按 workflowID 恢复 provider；UI first pass 已能创建/编辑 visual wait、在 Task Inspector 画 bounds、为 `pixelMatched` 提供 ColorPicker + hex/swatch、在 AI Draft Preview 从 package `visualAssets` 选择 region/image/baseline refs、登记 draft 同目录/子目录内的 package-local image/baseline 文件、把外部图片复制进 draft package 的 `assets/images` / `assets/baselines`、捕获 screen region 为 package-local baseline PNG，并编辑 visual draft 字段，Graph/Timeline/Inspector 也能展示 watched target/polling/timeout；仍缺更完整的 managed storage/迁移策略和产品级截图/录屏验收证据。 |
| AI 导入链路仍需产品化 | Engine/UI | `sparkle.workflow.draft.v1` validator、`workflow macros --json`、`workflow list/show/status/export`、`workflow draft init/inspect/normalize/patch/task add/task set/task remove/schedule set/condition set/dependency add/dependency set/dependency remove`、`workflow draft validate`、`workflow draft simulate`、`workflow import --dry-run`、`workflow import --confirm`、`workflow run/cancel/runs`、`workflow run/cancel --handoff app` 和 `workflow handoff status` 已有 first pass；UI 已有审阅后确认导入、导入后状态条、Undo/Restore、Refresh、Delete Workflow、OCR/visual condition quick-edit、schedule quick-edit、dependency quick-edit、task removal confirmation/undo、batch patch apply、单 workflow AI Draft export 和 workflow status projection summary first pass，但 daemon/background handoff、复杂 runtime projection 和批量 export/review polish 还没完成。 |
| 文档完成标记过度乐观 | Engine/UI | 旧 checklist 中部分 UI/package/picker 项已经标完成，但当前体验反馈显示不能作为真实完成态。 |

## 进行中的 Owner 2 页面优化

- 2026-07-06: Workflow 主布局已调整为左侧 workflow/macro palette、中央 FlowGraph + 下方 Resource Timeline、右侧 Inspector；timeline 不再挤在 inspector 下方。
- 2026-07-06: Macro Library 支持拖拽宏到 FlowGraph 画布生成 `AutomationTask`，并通过 accepted `upsertTask` action 保存 `graphPosition`；点击添加路径仍保留。
- 2026-07-06: Macro Library source-bin 视觉已有 first pass：macro row 从 `onTapGesture` 改为原生 `Button` + `onDrag`，静止态显示低对比 drag handle，hover 时才浮出 add affordance；点击仍是非拖拽辅助路径，拖入 workflow 后生成独立 `AutomationTask`，不改写 `SavedMacro`。
- 2026-07-06: FlowGraph 连线中点显示 trigger/delay 标签，替代只显示无语义图标；普通节点/边/toolbar/OCR authoring 控件移除无必要 `.controlSurface` / prominent 高亮。
- 2026-07-06: 产品视觉定调为 Pro 级 macOS 原生生产力工具：Workflow 页面静止态不能像 Web dashboard 或 Bootstrap 式大色块控件；普通控件退到背景里，用户的 graph、timeline、依赖和运行证据成为主视觉。
- 2026-07-06: FlowGraph 节点连接点已有直接 authoring first pass：用户从节点开始连线后可在画布顶部选择 trigger，点目标节点后通过 `AutomationAction.upsertDependency` 创建对应连线；边标签可右键删除，不必打开 Inspector。
- 2026-07-06: Task Inspector 增加非拖拽 authoring：基于 projection 位置微调 task、从当前 task 创建 dependency、查看 incoming/outgoing、选择或删除连线；Workflow 任务列表也能接收 Macro Library 拖放生成 task。
- 2026-07-06: Workflow Inspector 任务列表的 Macro Library 拖入已有插入线 first pass：空列表显示 drop target，已有任务列表在每个行间显示克制 insertion line，并按用户看到的位置把新 macro task 插入 `workflow.tasks` 顺序；该路径通过 `.upsertWorkflow` 提交静态 workflow，不调用 repository/runtime，也不改写 `SavedMacro`。
- 2026-07-06: AI Draft 入口已有 preview first pass：读取 `sparkle.workflow.draft.v1` 草稿，使用当前 Macro Library 生成 catalog，展示 task/dependency、validation errors/warnings、next actions、宏解析状态、模拟步骤、资源占用、分支决策和 import dry-run 编译预览；dry-run 通过后可在 sheet 内确认导入/替换，UI 通过 `.upsertWorkflow` 交给 reducer/runtime 持久化。
- 2026-07-06: AI Draft 导入后已有 rollback/refresh first pass：主页面显示导入/替换状态条，可 Refresh projection，可对新导入 workflow 执行 Undo Import，对替换导入执行 Restore Previous；Workflow Inspector 也提供确认式 Delete Workflow，所有动作仍走 reducer action。
- 2026-07-06: AI Draft preview 已接入 OCR condition quick-edit first pass：用户可在预览 sheet 内选择 OCR condition task，编辑文本、匹配模式、require visible、timeout 和 polling；应用后使用 `AutomationWorkflowDraftEditor.setCondition` 生成新的 in-memory draft，并重新生成 validation、simulation、import dry-run 和 compiled workflow 预览。该 UI 不写 draft 文件，不直接调用 CLI/repository/evaluator。
- 2026-07-06: AI Draft preview 已接入 schedule/dependency/task remove quick-edit first pass：用户可在预览 sheet 内把 task schedule 改为 Manual/Once/Repeating，修改 dependency 的 from/to/trigger/delay/enabled 或移除连线，并用确认弹窗删除 task；删除 task 会提示受影响依赖边数，并提供本地 Undo。应用后使用 `AutomationWorkflowDraftEditor.setSchedule` / `setDependency` / `removeDependency` / `removeTask` 更新 in-memory draft 并重建 validation、simulation、import dry-run 和 compiled workflow 预览。该 UI 不写 draft 文件，不直接调用 CLI/repository/scheduler。
- 2026-07-06: Workflow Inspector 已接入单 workflow AI Draft export first pass：用户可把当前 workflow 导出为 `sparkle.workflow.draft.v1` JSON；UI 使用 `AutomationWorkflowDraftExporter` 和当前 Macro Library catalog 生成 document，写入用户选择的 JSON 文件，并展示 lossy export warnings。该 UI 不调用 CLI，不写 app repository。
- 2026-07-06: Owner 1 已提供 `workflow import --confirm --json` first pass，命令通过 reducer `.upsertWorkflow` / `.persistWorkflows` 和 repository 写入 `automations.json`，不启动 Player，也不写 `SavedMacro` 运行态。
- 2026-07-06: Owner 1 已提供 draft editing CLI first pass：`workflow draft init`、`inspect`、`normalize`、`task add`、`task set`、`task remove`、`schedule set`、`condition set`、`dependency add`、`dependency set`、`dependency remove` 使用同一 `sparkle.cli.result.v1` envelope；edit 命令只在显式 `--out` 时写 draft 文件，不写 app repository。
- 2026-07-06: Owner 1 已提供 `workflow draft patch <draft.json> <patch.json> --json` first pass，可顺序应用 `sparkle.workflow.patch.v1` 的 add/set/remove/schedule/condition/dependency/normalize 操作；只有显式 `--out` 才写 draft 文件，不写 app repository。
- 2026-07-06: Owner 1 已提供 `workflow list/show/export --json` first pass，可把现有 workflow 导出为 `sparkle.workflow.draft.v1`，export 对 OCR region 等有损转换会输出 warnings。
- 2026-07-06: Owner 1 已提供 `workflow status [workflow-id] --json` first pass，可从 repository workflows + run history 输出 workflow/task 状态摘要和用户语言标签；该命令只读，不启动 Player、不取消 run、不获取资源。
- 2026-07-06: Owner 1 已提供 runtime control CLI first pass：`workflow run <workflow-id> --confirm --json` 通过 `AutomationRuntimeSession` 和 reducer `.manualStart` 启动，`workflow runs <workflow-id> --json` 读取历史，`workflow cancel <run-id> --confirm --json` 对 repository-visible 非终态 run 走 reducer cancellation。`run` 支持 `--repository-dir`、`--macros-dir`、`--wait-timeout` 和测试用 `--player-mode fakeSuccess`。
- 2026-07-06: Owner 1 已提供 runtime App-host handoff mailbox + receipt/status/result-readback first pass：`AutomationRuntimeHandoffCommand` / `AutomationRuntimeHandoffClient` 使用 `automation-runtime-handoff.json` 保存 manual-start/cancel-run 命令；CLI `workflow run/cancel --handoff app` 只投递命令并返回 handoff payload；`LiveAutomationRuntimeHost` 轮询 mailbox、通过现有 `AutomationRuntimeSession` dispatch action，并写入带 run IDs 的 `AutomationRuntimeHandoffReceipt`；CLI `workflow handoff status <command-id>` 可查询 pending/dispatched/failed/missing，并在 repository 中找到对应 run history 时返回 `runs` snapshots 和 `workflowStatus`。该能力不唤醒未运行 App，不提供 push-style CLI 进度流，也不是 daemon。
- 2026-07-06: Owner 1 已提供 next scheduled occurrence projection first pass：`AutomationScheduleOccurrence` 计算 once/repeating 下一次未被 run history 表示的计划时间，`AutomationTaskNodeProjection.nextScheduledOccurrence`、`AutomationWorkflowProjection.nextScheduledOccurrence` / `nextScheduledTaskID` 可供 UI 直接显示。
- 2026-07-06: Owner 1 已提供 repeating scheduled run generation reducer first pass：`AutomationScheduleOccurrence.nextDueOccurrence` 计算已到期但未表示的 occurrence，`AutomationReducer.clockTick` 会为 repeating task 创建独立 `AutomationTaskRun`，并通过既有 `prepareRun` 路径执行；每个 task 每次 tick 最多补一个 due occurrence，避免长时间停机后洪泛。
- 2026-07-06: Owner 1 已提供 resource waiting queue/resume reducer first pass：`AutomationReducer.resourceLeaseDenied` 不再把 busy 资源完成为 `.resourceConflict`，而是保持 `waitingForResource`；`clockTick`、terminal release 和 panic release 会通过 `.requestResource` 重试等待 run，且先重试较早创建的等待 run，再创建/推进新的下游工作。
- 2026-07-06: Owner 1 已提供 resource max-wait first pass：`AutomationResourceRequirement.maxWaitDuration` / draft `maxResourceWaitSeconds` / CLI `--max-resource-wait` 可以让等待资源的 run 到期后完成为 `.timedOut(deadline:)`，走既有 timeout branch 或 retry 策略。
- 2026-07-06: Owner 1 已提供 resource waiting reason projection first pass：`AutomationTaskNodeProjection.resourceWaiting` 和 `AutomationResourceTimelineItem.resourceWaiting` 暴露 detail、resources/resourceLabels、priority/priorityLabel、waitingSince/waitedDuration、可选 max-wait deadline/remaining/fraction 和 active lease blockers，UI 不需要在 SwiftUI 中读取 leases 或重建资源队列解释。
- 2026-07-06: Owner 1 已提供 timeout watchdog reducer first pass：`AutomationReducer.clockTick` 会检查 queued/running 且超过 `AutomationTask.timeout` 的 run，宏任务先发 `.cancelPlayer`，再通过既有 terminal path 完成 `.timedOut(deadline:)`、释放 lease、持久化 run，并触发 `.onTimeout` 下游依赖；等待资源阶段不计入 task runtime timeout。
- 2026-07-06: Owner 1 已提供 retry policy reducer first pass：failure/timeout 终态会按 `AutomationRetryPolicy.maxAttempts` 创建同一 execution 的下一次 `AutomationTaskRun`，递增 attempt，保留每次 run/evidence；fixed/exponential backoff 通过 planned retry run 和 `earliestStartTime` 表达，只有 attempts 用完后才解析 failure/timeout downstream。
- 2026-07-06: Owner 1 已提供 runtime feedback projection first pass：`AutomationTaskNodeProjection` 和 `AutomationResourceTimelineItem` 都带有 `timeoutCountdown` 与 `retryAttemptSummary`，UI 可在 graph/timeline/inspector 中展示剩余时间、elapsed fraction、当前 attempt、最大 attempts、剩余 attempts 和下一次 retry 时间，而不需要在 SwiftUI 中重新推导 run history。
- 2026-07-06: Owner 1 已提供 join policy first pass：`AutomationTask.joinPolicy` 支持 `all`、`any`、`firstMatched`；reducer 会按目标 task 的策略解析多入边依赖；draft JSON、draft editor、patch、import/export 和 `workflow draft task add/set --join-policy` 都支持该字段；`AutomationTaskNodeProjection.joinPolicy` / `joinPolicyLabel` 可供 UI 直接显示。
- 2026-07-06: Owner 1 已提供 visual condition core spec first pass：`AutomationConditionKind.visual` 支持 `regionChanged`、`imageAppeared`、`imageDisappeared`、`pixelMatched`；`AutomationVisualCondition` 保存 `regionRef`、`imageRef`、`baselineRef`、`pixel`、`colorHex` 和 `threshold`；draft validator/import/export/editor/patch 与 CLI `workflow draft condition set` 都保留这些字段；condition evaluator 可注入 visual provider。
- 2026-07-06: Owner 2 已把 workflow-level status/statusDetail 汇总放入 `AutomationWorkflowProjection`，左侧 workflow row 和 Workflow Inspector 可显示整体 running/waiting/attention 摘要；该 UI 只读 projection，不调用 CLI 或 runtime control。
- 2026-07-06: Owner 2 已把 AI Draft batch patch 接入 preview sheet first pass：用户可选择 `sparkle.workflow.patch.v1` JSON，在当前草稿预览内顺序应用 patch operation，查看 changed task/dependency keys，并自动重建 validation、simulation、import dry-run 和 compiled workflow 预览。该 UI 不写 draft 文件，不直接调用 CLI/repository/scheduler。
- 2026-07-06: Owner 2 已把 Task Inspector 的 App 内运行控制做成确认式 first pass：Run Task 通过 `AutomationViewIntent.startTask` 映射到 reducer `.manualStart`，Cancel Run 通过 `.cancelRun`，前台输入任务会在确认弹窗里提示可能控制鼠标键盘，禁用 task 不可直接运行；Delete Task 也改为确认式。该 UI 不调用 Player/scheduler/repository，只发 accepted reducer action。
- 2026-07-06: Owner 2 已补齐 resource waiting projection/fixture first pass：`waitingForResource` 不再在 UI projection 中显示成 queued，Graph/Timeline/Inspector/Run History 会读取 `.waiting` 和 `resourceWaiting` 用户语言；OwnerC fixture 增加 foreground-input resource wait 与 planned retry 样本，`AutomationViewProjectionTests` 覆盖该 review state。
- 2026-07-06: Owner 2 已把 Graph 节点和 Resource Timeline 的运行态反馈推进到同源 projection first pass：节点和 timeline item 都用 `AutomationDisplayStatus` 显示克制 hairline/status chip，避免整卡高饱和填色，也不在 SwiftUI 中重新推导 runtime 语义。
- 2026-07-06: Owner 2 已消费 timeout/retry runtime projection first pass：Graph node 与 Resource Timeline row 读取 `timeoutCountdown` / `retryAttemptSummary`，展示剩余时间、elapsed progress、attempt 摘要和 planned retry 时间；SwiftUI 只格式化 projection 字段，不扫描 run history、不重算 timeout deadline。
- 2026-07-06: Owner 2 已把同一份 timeout/retry runtime projection 接入 Task Inspector first pass：选中 task 后右侧 Run Status 区显示 projection-backed status、deadline、timeout duration、attempt、remaining attempts、planned retry 和 evidence/next schedule 摘要；Inspector 不扫描 run history、不直接调用 runtime。
- 2026-07-06: Owner 2 已把 FlowGraph 连接点拖线 authoring 推进到 first pass：节点右侧显示克制 connector handle，用户可从 handle 拖出临时虚线并落到目标 task 创建 dependency；落点失败会取消 pending link，点击式连接和 Inspector 非拖拽路径仍保留。
- 2026-07-06: Owner 2 已消费 next scheduled occurrence projection first pass：Workflow row、Workflow Inspector、FlowGraph node 和 Resource Timeline 都显示下一次计划时间，Inspector/timeline 会显示下一次启动的 task 名称；SwiftUI 只读 projection，不解析 once/repeating schedule、不扫描 run history。
- 2026-07-06: Owner 2 已消费 join policy projection first pass：`AutomationTaskNodeProjection` 增加 incoming dependency count，OwnerC fixture 包含 `.any` 多入边合流节点；FlowGraph node 只在多入边或非默认策略时显示克制 join badge，Task Inspector Advanced 区可通过 `.upsertTask` 编辑 `all` / `any` / `firstMatched` 并展示用户说明。
- 2026-07-06: Workflow 产品视觉合同已补强为硬验收门槛：App 定位是 Pro 级 macOS 原生生产力工具，普通 `.controlSurface` 全宽强背景和高饱和大色块不能作为默认控件语言；视觉层级必须让用户数据和运行状态先于普通命令；Macro Library 是 source bin，拖入后生成独立 `AutomationTask` 实例；中心依赖图、If/Then 条件表达、运行时呼吸描边/进度 hairline/状态点反馈是下一阶段 UI 的核心验收对象。
- 2026-07-06: Workflow visual/UX contract 已增加明确的 Contract Snapshot、`.controlSurface` 使用政策、所见即所得拖拽编排规则、依赖图 Level 0/1/2 和 runtime feedback 最低形态。Owner 2 后续 UI 不能只以“功能按钮可点”验收，必须证明 source-bin 拖入、图上连线、条件分支可读和运行时 graph/timeline/inspector 同步反馈。
- 2026-07-06: Owner 1 已提供 visual condition progress projection first pass：`AutomationTaskNodeProjection.conditionProgress` 和 `AutomationResourceTimelineItem.conditionProgress` 能描述条件类型、目标/细节文案、polling interval、active polling、condition timeout countdown，以及 visual refs；OwnerC fixture 包含 running visual wait 样本，UI 不需要在 SwiftUI 中拆 `AutomationConditionKind`。
- 2026-07-06: Owner 1 已提供 live visual condition evaluator first pass：`AutomationConditionEvaluatorClient.live` 默认接入 `AutomationVisualConditionEvaluatorClient.live`；provider 会 capture display，复用 visual/OCR search-region resolution，pixel/color 条件可直接对 live screenshot 采样，image appeared/disappeared 与 regionChanged 可通过 injected template/baseline providers 评估；缺失 image/baseline/color 配置会返回用户可读 rejected outcome，而不是无声轮询。
- 2026-07-06: Owner 1 已提供 visual asset/region registry for AI drafts first pass：`AutomationWorkflowDraftDocument.visualAssets` 可声明 package-level regions/images/baselines；validator 会检查 duplicate/invalid visual assets 和缺失的 region/image/baseline refs；import 会把 OCR/visual `regionRef` materialize 成 `AutomationOCRCondition.searchRegion` 或 `AutomationVisualCondition.searchRegion`；export 会把 internal concrete OCR/visual regions 写回 `visualAssets.regions`，避免 region bounds 丢失成 lossy warning。该能力是 draft/package 合同；baseline capture、package-root retention 和 AI Draft Preview 外部图片复制导入由独立 first pass 补上，更完整的 managed storage/迁移策略仍未完成。
- 2026-07-06: Owner 1 已提供 workflow visualAssets persistence + package provider binding first pass：`AutomationWorkflow.visualAssets` 会随 draft import/export 和 `.sparkrec_workflow` package round-trip 保留 regions/images/baselines；`AutomationVisualAssetImageProviders.workflowPackages(...)` 可按 `workflowID` 把 package-local `visualAssets.images` / `visualAssets.baselines` 解析成 live evaluator 可注入的 image/baseline providers；`LiveAutomationRuntimeHost.visualAssetPackages` 已能接收 workflow-scoped package roots。package-root retention、baseline capture 和 draft-package 外部图片复制导入已由独立 first pass 补上，更完整的 managed storage/迁移策略仍未完成。
- 2026-07-06: Owner 1 已提供 visual asset package-root retention first pass：新增 `automation-visual-asset-roots.json` 本地 manifest、`AutomationVisualAssetPackageRootClient` 和 file/in-memory stores；CLI `workflow import --confirm` 会把带 image/baseline refs 的 draft 源目录登记为 package root，支持 `--visual-assets-root` 覆盖；AI Draft Preview 确认导入和 `.sparkrec_workflow` package import 也会登记或清理对应 workflow root；`LiveAutomationRuntimeHost` 会从 manifest + repository workflow.visualAssets 动态恢复 request-aware providers。该能力不复制外部文件，也不解决迁移/丢失文件的存储策略。
- 2026-07-06: Owner 2 已消费 visual condition core/progress first pass：Workflow Inspector 可添加 Screen text / Visual wait 条件，Task Inspector 可编辑 visual type、regionRef、screen bounds、image/baseline/pixel/color/threshold，并通过 `.upsertTask` 保存；AI Draft Preview 的 condition quick-edit 可编辑 OCR 或 visual draft 字段并重建 validation/simulation/import dry-run；Graph/Timeline/Task Inspector 会读取 `conditionProgress` 展示 watched target、polling cadence 和 condition timeout。该 UI 不调用 evaluator/provider/CLI/repository。
- 2026-07-06: Owner 2 已补齐 existing task list reorder first pass：Workflow Inspector task row 可作为 drag source 拖到行间 insertion line，或用上/下 icon button 精确移动到相邻位置；UI 只重写 `workflow.tasks` 顺序并通过 `.upsertWorkflow` 提交，不改依赖、不写宏库、不调用 repository/runtime。列表重排现在会在有 projection 邻居时更新 moved task 的 `graphPosition`，避免 list 顺序和 graph 位置长期脱节。
- 2026-07-06: Owner 2 已加入 FlowGraph branch guide first pass：中心画布会把 source 节点的 outgoing edges 聚成克制的 Then / Else / Timeout / Cancel / Always 摘要，直接显示目标 task 和 delay/trigger 辅助文案；该 UI 只读现有 node/edge projection，不改 reducer/core 合同，也不拦截现有 edge label 或 connector 交互。
- 2026-07-06: Owner 2 已加入 Task Inspector If/Then/Else branch panel first pass：选中 condition task 时，Inspector 在 condition 编辑区下方显示 Then、Else、Timeout 分支，以及已有的 Always/Cancel 分支；每条分支可选择 dependency、跳到 target task、删除 dependency，并在有 `branchDecision` projection 时显示 triggered/skipped/waiting/disabled、outcome、decidedAt、source/target run 绑定和 detail 摘要；所有写入仍走 `.deleteDependency`，不重算 reducer branch semantics。
- 2026-07-06: Owner 2 已加入 Task Run Detail first pass：Task Inspector 的 Run History 行现在可选中并展开 run detail，显示 run/execution/attempt/upstream、timeline timestamps、evidence ID，以及 failed/timed out/cancelled/permission/missing macro 等 next-check 文案；retry policy 用于提示 no retry attempts remain。Run Detail 现在也有 per-run-aware evidence viewer first pass：用户可按需读取该 macro package 的 `runs/<evidenceID>/manifest.json` / `report.json`，找不到时才降级到 matching legacy latest evidence；UI 会显示 source、binding status、diagnostic focus/preview/next-check 摘要、manifest created/run/macro/file 绑定信息、report payload、inline screenshot preview，并 Reveal report / Open screenshot；如果截图文件存在但预览数据不可读，UI 会保留 report payload 并显示 preview unavailable，而不是让整段 evidence 加载失败；该 UI 通过 app-edge presenter 读 macro package evidence，不直接调用 Player/scheduler/runtime/evaluator。Run Detail 也会优先从 `AutomationTaskRun.branchEvidence` 显示与当前 run 相关的 durable incoming/outgoing branch evidence，包含 dependency、trigger、target run、delay、target join policy 和 reason；旧 run 则回退到 `AutomationDependencyEdgeProjection.branchDecision`。Evidence readiness 会把 branch evidence 标为 durable 或 fallback。Owner 1 已补 `AutomationTaskRun.conditionEvidence` first pass，Run Detail 可直接显示 OCR/visual condition 的 observed summary、sample count、region、score/threshold、diagnostic fields 和 live sample artifact refs；Owner 2 已补 fixture visual diagnostics drill-in 截图，live capture 录屏仍未完成。
- 2026-07-06: Owner 1 已加入 visual diagnostics artifact references first pass：`AutomationConditionDiagnosticArtifact` 进入 `AutomationConditionEvaluationEvidence`，live OCR/visual evaluator 会把最后一次 display sample 和 watched-region crop 写到 App Support 的 `AutomationEvidence/<runID>/...`，run history 只持久化相对路径和 image metadata；失败/拒绝终态也会尽量写入解释性 diagnostics payload，能拿到截图时保留 sample/crop refs；旧 condition evidence 没有 `artifacts` 字段时解码为空数组。
- 2026-07-06: Owner 2 已补 Product Evidence fixture screenshot first pass：新增 CLI `workflow product-evidence snapshot`，可用 `AutomationRunState.ownerCFixture` 生成 `idle-workflow.png`、`drag-link-authoring.png`、`task-reorder-authoring.png`、`running-workflow.png`、`failed-run-detail.png`、`failed-run-preview-unavailable.png`、`visual-diagnostics-drill-in.png` 和 `branch-evidence-drill-in.png`；每张图都有同名 `.md` 说明。截图同时推动 Workflow surface token 克制化：普通 section/subsurface 和 Workflow buttons 使用暗色低对比样式，避免 snapshot/无窗口渲染下出现白色大块控件。`failed-run-detail.png` 现在展示 Reveal/Open 文件动作按钮和 Reveal Report 反馈状态；`visual-diagnostics-drill-in.png` 现在展示 visual artifact Open/Reveal 按钮和 Artifact revealed 反馈状态。完整真实 drag/reorder 录屏、Open/Reveal 交互录屏和 live capture 录屏仍未完成。
- 2026-07-06: Owner 2 已加入 visual pixel color picker first pass：Task Inspector 和 AI Draft Preview 复用的 `AutomationVisualConditionEditorView` 在 `pixelMatched` 模式下提供原生 ColorPicker、颜色 swatch 和规范化 hex 输入；该 UI 只写 `targetColorHex` / draft `colorHex` 字段，不调用 evaluator/provider，也不声明 template/baseline live provider 已完成。
- 2026-07-06: Owner 2 已加入 draft/package visual asset picker first pass：共享 `AutomationVisualConditionEditorView` 的 region/image/baseline reference 字段在 AI Draft Preview 中会读取 `document.visualAssets.regions/images/baselines`，用原生 Picker 选择已声明 package assets，同时保留 custom ref TextField；Task Inspector 没有 package registry 时继续只写手动 refs。该 UI 不读取 package files、不调用 live provider、不捕获 baseline。
- 2026-07-06: Owner 2 已加入 draft/package visual asset file registration first pass：AI Draft Preview 的 visual assets 区会保留 draft 源目录作为 package root，允许选择该目录或子目录内的 image/baseline 文件并登记为 `visualAssets.images/baselines` 的安全相对路径，随后重建 validation/simulation/import dry-run；包外文件会被拒绝。该 file-registration UI 不复制文件、不调用 live provider；runtime 绑定由导入后的 package-root retention first pass 负责。
- 2026-07-06: Owner 2 已加入 AI Draft Preview baseline capture first pass：用户可在打开本地 draft/package 后从 Visual Assets 区点击 Capture Baseline，画选 screen region 后保存为 `assets/baselines/<key>.png`，写入 SHA-256 metadata，并登记到 `visualAssets.baselines` 后重建 validation/simulation/import dry-run。该 UI 不复制外部 template，也不声明 live evaluator evidence 已完成。
- 2026-07-06: Owner 2 已加入 AI Draft Preview external visual asset import first pass：Visual Assets 区可用 Import File 选择 package 外部图片，按当前 Image/Baseline 类型复制到 `assets/images/<key>.<ext>` 或 `assets/baselines/<key>.<ext>`，写入 SHA-256 metadata，并登记为 package-relative `visualAssets.images/baselines` 后重建 validation/simulation/import dry-run。该 UI 是 draft package 内复制策略 first pass，不解决移动/删除源文件后的 managed storage 迁移，也不声明 live evaluator evidence 已完成。
- 仍未完成：完整真实 drag/reorder 产品验收录屏、Open/Reveal 交互录屏、daemon/background runtime handoff、批量 draft export/更深 export review UI、复杂 resource queue 的 live projection、更完整 timeout/retry 运行态录屏证据、managed visual asset storage/迁移策略、visual condition live 运行态录屏证据。

## 下一阶段目标

1. 冻结 Workflow 页面用户模型和 projection/action 合同。
2. 让 Engine owner 每完成一个能力，都产出 UI 可消费的 projection 字段、action、fixture 和测试证据。
3. UI owner 基于 fixture 和 live projection 并行重做页面，不在 SwiftUI 内实现状态机。
4. 继续完善 AI workflow draft schema；当前已交付只读 macro catalog、workflow list/show/status/export、draft editing/patch first pass、validator、deterministic simulate、import dry-run、confirmed import CLI、runtime control CLI first pass、runtime App-host handoff mailbox + receipt/status/result-readback first pass、next scheduled occurrence projection + UI display first pass、repeating scheduled run generation reducer first pass、resource waiting queue/resume first pass、resource waiting reason projection first pass、timeout watchdog reducer first pass、retry policy reducer first pass、runtime feedback projection first pass、runtime branch decision evidence first pass、condition diagnostics evidence + live sample artifact refs first pass、join policy reducer/draft/projection first pass、join policy Graph/Inspector first-pass UI、FlowGraph branch guide first pass、Task Inspector If/Then/Else branch panel first pass、visual condition core/draft/evaluator-provider first pass、visual condition progress projection + UI rendering first pass、live visual evaluator first pass、visual asset/region registry for AI drafts first pass、workflow visualAssets persistence + package provider binding first pass、visual asset package-root retention first pass、visual pixel color picker first pass、draft/package visual asset picker first pass、draft/package visual asset file registration first pass、AI Draft Preview baseline capture first pass、AI Draft Preview external visual asset import/copy first pass、UI 确认导入、导入后 rollback/refresh、OCR/visual condition quick-edit、schedule quick-edit、dependency quick-edit、task removal confirmation/undo、batch patch apply、App 内 Task run/cancel 确认式控制、Graph/Timeline 同源状态反馈、timeout/retry Graph/Timeline/Task Inspector first-pass rendering、Task Run Detail first pass、Task Run per-run-aware evidence viewer first pass、idle/running/connector drag-link/task-reorder/failed-run/preview-unavailable/branch/visual diagnostics fixture product evidence、单 workflow AI Draft export 和 workflow status projection summary first pass，下一步是 daemon/background runtime handoff、完整真实 drag/reorder 录屏、managed visual asset storage/迁移策略和更完整 timeout/retry/visual condition live 运行态录屏证据；MCP 暂缓，不和 CLI 并行开发。
5. 只在用户能完成核心链路后，才把 checklist 标为 done。
