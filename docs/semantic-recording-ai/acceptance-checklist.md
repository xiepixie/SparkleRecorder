# Acceptance Checklist

更新时间：2026-07-07
状态：规划验收清单

只有代码存在、测试通过、文档匹配真实行为，并且有产品证据时，才能把条目标为 done。

当前状态和执行顺序维护在 [06-current-work-and-next-tasks.md](06-current-work-and-next-tasks.md)，S0-S4 最终差距维护在 [14-s0-s4-final-gap-alignment.md](14-s0-s4-final-gap-alignment.md)。本清单只标记可验收事实；不能因为方向文档、fixture 或 mock 通过就把 live-product 条目标为 done。

## Program Gap Alignment

- [x] Maintain a single S0-S4 final gap alignment document that states the current acceptance posture for each owner. Evidence: [14-s0-s4-final-gap-alignment.md](14-s0-s4-final-gap-alignment.md) records S0 closed, S1 first pass closed, S2 active blocker, S3 paused and S4 product-ready live work paused.
- [x] S3 first pass is explicitly paused rather than treated as the next active feature stream. Evidence: [workstreams/s3-review-ux-evidence-editing.md](workstreams/s3-review-ux-evidence-editing.md), [workstreams/README.md](workstreams/README.md), [06-current-work-and-next-tasks.md](06-current-work-and-next-tasks.md) and [DOCUMENTATION_STATUS.md](../DOCUMENTATION_STATUS.md) all gate further S3 installed-app work on S2 live bundle evidence.
- [x] S4 product-ready live work is explicitly paused until S2 live bundle evidence, default root policy and Review alignment are stable. Evidence: [workstreams/s4-cli-ai-app-knowledge.md](workstreams/s4-cli-ai-app-knowledge.md), [14-s0-s4-final-gap-alignment.md](14-s0-s4-final-gap-alignment.md), [12-remaining-work-and-direction-control.md](12-remaining-work-and-direction-control.md) and [DOCUMENTATION_STATUS.md](../DOCUMENTATION_STATUS.md).
- [x] Acceptance checklist distinguishes strict S0 closure, S1 contract first pass, S2 live-product blockers, S3 fixture/stored first pass and S4 fixture/explicit stored-bundle first pass without marking missing live evidence complete. Evidence: this checklist keeps live S2/S3/S4 items open below while marking only documented/tested first-pass or fixture/stored facts done.
- [ ] S2 live semantic recording product evidence is accepted as the unblocker for S3/S4. Required evidence: authorized live `.mov` / keyframe / sidecar bundle, readiness diagnostics, OCR/window/AX observations where required, `SavedMacro.semanticRecording` from ordinary Recorder bridge, recording-start guidance evidence and sidecar-reviewed product notes.

## S0-S4 Final Gap Coverage

This section is the checklist coverage map for [14-s0-s4-final-gap-alignment.md](14-s0-s4-final-gap-alignment.md). It does not replace the detailed phase items below; it prevents a future review from calling the program complete while one final gap is only described in prose.

- [x] S0 strict Workflow evidence closure is represented as closed without implying semantic recording live capture is done. Evidence: Bridge Phase items below cite the four strict live clips and `workflow product-evidence audit --require-live --json` 13/13 result; S2 live capture items remain open.
- [x] S1 contract closure is represented as first-pass complete, with future schema changes requiring an interface request. Evidence: Phase 0 contract items are checked only for schema/safe-ref/fixture facts, while S2 live producer and S3/S4 product gates remain open.
- [ ] S2 authorized live bundle gate is accepted. Required evidence: macOS 15+ live bundle with real `.mov`, event-aligned keyframes, manifest/sidecars, persisted reload diagnostics, readiness status, OCR/window/AX observations when required and reviewed evidence sidecar.
- [ ] S2 ordinary Recorder bridge gate is accepted. Required evidence: installed app recording with `semanticRecordingEnabled`, preflight before countdown, normal stop saving a playable macro plus `SavedMacro.semanticRecording`, discard/cancel/failure cleanup of temporary bundles and linked Macro Review opening from the saved macro.
- [ ] S2 safety/privacy/cleanup gate is accepted. Required evidence: Secure Input/password/excluded-target suppression live proof, redacted frame/video sidecar consumption proof, playback-preserving readable metadata withholding proof, reviewed text-anchor mutation decision and live manual/scheduled cleanup product evidence.
- [ ] S2 product root/id policy gate is accepted. Required evidence: default semantic recording root, stable recording id policy for saved macros and later workflow runs, root/catalog audit behavior and documentation of when explicit bundle paths are no longer required for product flows.
- [ ] S3 installed-app Review gate is accepted. Required evidence: real saved-macro-linked bundle opens from Run Detail/Macro Review without an open-panel fallback, shows Bundle Health / Run Target / evidence tiles, and uses presenter-mediated Open/Reveal actions.
- [ ] S3 live frame-to-condition and Draft Preview gate is accepted. Required evidence: live OCR wait, image appeared/disappeared, region changed or pixel condition creation from a recorded frame, package-local asset materialization, Review accept/reject behavior and Draft Preview -> confirmed import evidence.
- [ ] S4 product-ready live catalog/query gate is accepted. Required evidence: default/live `recording list/show/explain/frames/frame show/events-near/ocr search/visual search --json` over accepted S2 bundles, safe refs only by default, artifact availability/missing status and no whole video/image byte output by default.
- [ ] S4 stored/live suggestion and draft synthesis gate is accepted. Required evidence: stored/live `recording suggest waits/locators/conditions/cleanup --json` with real suggestion synthesis, evidence refs, confidence/risk/fallback, `availability` semantics, missing/deleted artifact status and product-ready stored/live `workflow draft from-recording --json` preserving Review/Draft Preview mutation boundaries.
- [x] App Knowledge remains explicitly deferred until accepted live bundles, product-ready catalog/search and Review/Draft Preview mutation boundaries exist. Evidence: Phase 6 items below remain open, while S4 workstream and final-gap doc state App Knowledge starts after enough accepted live bundles exist.

## Bridge Phase: Workflow Evidence Closure

- [x] Capture live visual diagnostics run from the installed App, including last-sample/watched-region artifact preview. Evidence: `docs/workflow-page-productization/product-evidence/live-visual-diagnostics-open-reveal.mov` and `.md`.
- [x] Capture real Open/Reveal interactions for visual diagnostic artifacts through the app-edge presenter/file-action path. Evidence: `docs/workflow-page-productization/product-evidence/live-visual-diagnostics-open-reveal.mov` and `.md`.
- [x] Capture real Reveal Report / Open Screenshot interactions for macro run evidence. Evidence: `docs/workflow-page-productization/product-evidence/live-macro-evidence-open-reveal.mov` and `.md`.
- [x] Capture real branch evidence consistency for the strict S0 gate. Evidence: `docs/workflow-page-productization/product-evidence/live-branch-evidence-consistency.mov` and `.md` prove App-host handoff run payload consistency across source run, target run, dependency trigger and live App window capture; richer manual Run Detail drill-in recording remains optional/future.
- [x] Accept template/baseline preview refs as source-frame/runtime-sample evidence, not just string fields. Accepted contract: [09-template-baseline-preview-refs.md](09-template-baseline-preview-refs.md), [workstreams/s1-contract-core.md](workstreams/s1-contract-core.md).
- [x] Render template/baseline source-frame/runtime-sample/decision evidence in a fixture artifact before claiming the fixture-level preview-ref handoff complete. Evidence: `docs/workflow-page-productization/product-evidence/template-baseline-preview-refs.png` and `.md`.
- [x] Render the same source-frame/runtime-sample/decision evidence inside the real Macro Review or Run Detail UI before claiming the user-facing preview-ref experience complete. Evidence: `docs/workflow-page-productization/product-evidence/semantic-review-timeline.png`, `semantic-review-stored-bundle.png`, `semantic-review-run-detail.png` and paired sidecars render Source/Runtime/Diff evidence through `SemanticRecordingReviewPresenter` / `SemanticRecordingReviewFixtureView`; live installed-app recording evidence remains a separate S3 gate.
- [ ] OCR/visual region picker renders wait/verify targets as region boxes with clear labels, and reserves click circles/pulses for actual click actions. Partial code evidence 2026-07-07: editor coordinate preview now routes wait/verify through `ActionPreviewAffordance` condition-region labels and routes click/text-click through click pulse affordances; product screenshot/recording evidence remains required before checking this item.
- [x] Capture real drag/reorder or drag-link clip proving indicator and reducer mutation match. Evidence: `docs/workflow-page-productization/product-evidence/live-task-reorder-wysiwyg.mov` and `.md`.
- [x] Keep fixture evidence sidecars explicit about what is fixture-proven versus live-product-proven. Evidence: `docs/workflow-page-productization/product-evidence/README.md` capture rules plus the `semantic-review-*.md` and `semantic-recording-cli-low-token-transcript.md` sidecars label fixture/stored fixture/CLI evidence and known live-product gaps; strict live audit still rejects fixture/mock/synthetic sidecars.
- [ ] Action preview/grouping follows the user-facing semantics from `13-direction-decision-and-remaining-slices.md`: wait/verify render region boxes, click actions render click affordances, and clicks separated by meaningful waits or visual state changes are not merged into multi-click.
  Partial code evidence 2026-07-07: `ActionGroupProjection.textTargetGroups` lets selected recorded coordinate clicks become text-click targets while keeping wait/verify semantic text targets separate; `TextTargetReadiness` prevents empty inserted Click Text / Wait Text anchors from rendering as ready target cards while still allowing typed non-empty text with no observed frame; `ActionPreviewAffordance` / `ActionGroupProjection.previewAffordance` separates click pulses from wait/verify condition regions at projection level; `EventGrouper` and `ActionRowView` label empty/missing-anchor text-target actions as `needs text` / `No target text`, and incomplete text clicks are no longer mergeable into ordinary coordinate multi-clicks. `Localizable.xcstrings` contains the new English and Simplified Chinese strings for these states. Product evidence for the full preview/grouping gate remains open.
- [ ] Workflow orchestration supports explicit loop semantics without encoding loops as dependency cycles. Current state: scheduled/repeating starts and macro playback loops exist, but `AutomationWorkflow.validationIssues()` rejects graph cycles, so loop body / repeat-until / foreach semantics need a first-class contract before acceptance.
- [x] `workflow product-evidence capture-plan` lists missing S0 live gates, accepted clip filenames, sidecar template commands and missing labels.
- [x] `workflow product-evidence capture-plan` lists post-recording `complete-sidecar` command templates for each live gate option.
- [x] `workflow product-evidence prepare-live-capture` writes missing sidecar drafts while preserving existing notes by default, without satisfying strict live evidence audit.
- [x] Missing S0 live sidecar drafts are materialized under `docs/workflow-page-productization/product-evidence/`; placeholders remain intentionally incomplete until live clips are captured.
- [x] `workflow product-evidence complete-sidecar` fills reviewed sidecar fields through typed CLI options, validates accepted clip filenames, and leaves the gate open when the clip file is absent.
- [x] Strict product-evidence audit rejects zero-byte or size-unknown live `.mov` / `.mp4` clips, and `capture-plan` reports undersized clip paths instead of letting placeholder files close S0.
- [x] Strict product-evidence audit rejects live sidecars whose `Evidence source:` describes fixture/mock/synthetic evidence or whose `Clip file:` does not name exactly one accepted candidate matching the satisfying clip.
- [x] Strict product-evidence audit rejects live sidecars whose `Checklist item:` does not name the matching live gate title and id.
- [x] Strict product-evidence audit rejects non-video files renamed to live `.mov` / `.mp4`, and `capture-plan` reports invalid clip container paths.
- [x] Keep `06-current-work-and-next-tasks.md` updated whenever evidence status, owner boundary, accepted contract, or immediate next slice changes. Evidence: 2026-07-07 maintenance records S0 strict closure, S3 first-pass pause, S4 explicit stored-bundle/fixture CLI state, remaining product-ready blockers and the current S2 live-bundle next-slice queue.
- [x] Keep `12-remaining-work-and-direction-control.md` updated whenever the P0-P4 queue, overdesign decisions, or "do / do not do yet" boundary changes. Evidence: 2026-07-07 maintenance keeps P0-P4 status aligned with S3 pause, S4 low-token transcript, no-evidence suggestion guard and remaining live/product-ready blockers.
- [x] Keep `13-direction-decision-and-remaining-slices.md` updated whenever the user-facing slice plan, overdesign boundary, or remaining task grouping changes. Evidence: 2026-07-07 maintenance keeps Slice A-E aligned with S3 pause, S4 fixture/review-only draft gates, CLI safety and product-ready deferrals.
- [x] Keep `14-s0-s4-final-gap-alignment.md` updated whenever S0-S4 acceptance posture changes. Evidence: 2026-07-07 maintenance records the final gap state after S3 first pass: S0 closed, S1 first pass closed, S2 active blocker, S3 paused and S4 product-ready live work paused.
- [x] Keep [workstreams/s0-workflow-evidence.md](workstreams/s0-workflow-evidence.md) updated while S0 is active. Evidence: S0 workstream now records the strict gate as closed/current and keeps the future Cookie Run bound-window acceptance separate from S0 semantic recording closure.
- [x] `workflow product-evidence audit --require-live --json` passes before S0 Workflow Evidence Closure is claimed. Evidence: `.build/debug/SparkleRecorder workflow product-evidence audit --require-live --json` returned `allRequiredPresent: true`, `satisfiedRequiredCount: 13`, `requiredCount: 13`.

## Phase 0: Contract Freeze

- [x] Confirm S1/S2/S3/S4 owner boundaries from `08-parallel-workstreams.md` before implementation PRs split.
- [x] Define `SemanticRecordingBundle` schema.
- [x] Define `RecordingFrameReference`, `RecordingVisualObservation`, `RecordingSemanticEvent`.
- [x] Define private `timeline.jsonl`, AI-safe `events.jsonl`, and `suppressed.jsonl` boundaries.
- [x] Define Apple API policy: macOS 15+ baseline, default `SCRecordingOutput` video, event-aligned keyframes, no macOS 14 fallback.
- [x] Provide deterministic semantic recording fixture bundle/query/suggestion shape for S2/S3/S4 prototypes.
- [x] Define first-pass recording retention/deletion planning policy. Evidence: `SemanticRecordingRetentionTests` covers fresh/protected retention, expired artifact pruning with preserved metadata files, and explicit bundle deletion planning.
- [x] Wire first-pass retention settings to policy mapping and Settings UI. Evidence: `SemanticRecordingRetentionSettings` maps selected retention days plus expired disposition into `SemanticRecordingRetentionPolicy`, `AppState` persists the settings, `SettingsPanel` exposes the visual evidence retention controls, and `SemanticRecordingRetentionTests` covers the policy mapping.
- [x] Define pure retention/deletion confirmation projection. Evidence: `SemanticRecordingRetentionPresenter` and `SemanticRecordingRetentionTests` cover retained/protected, expired artifact prune and full bundle deletion presentations, destructive actions, keep fallback, artifact counts and preserved metadata counts.
- [x] Wire retention/deletion planning to app-edge dry-run/file removal. Evidence: `RecordingBundleStore.applyRetentionPlan` reports planned/candidate/deleted/missing/preserved paths, prunes only safe non-directory artifact files, and can delete the canonical bundle directory after confirmation.
- [x] Wire first-pass manual retention/deletion SwiftUI confirmation. Evidence: `SemanticRecordingRetentionCleanupPresenter` previews destructive plans, `SettingsPanel` exposes Review cleanup under the experimental visual-evidence setting, and `MenuBarController` applies the confirmed preview through `RecordingBundleStore.applyRetentionCleanup`.
- [x] Wire first-pass scheduled retention cleanup. Evidence: `SemanticRecordingScheduledRetentionCleanupPlanner` makes deterministic skip/run decisions from retention settings, last-run time and minimum interval, `AppState` persists the last scheduled cleanup time, `MenuBarController` invokes the existing `RecordingBundleStore.retentionCleanupPreview` / `applyRetentionCleanup` path on launch when eligible, and `SemanticRecordingRetentionTests` covers the pure scheduler decision.
- [ ] Capture live cleanup product evidence for manual and scheduled retention cleanup.
- [x] Update format/versioning docs when schema is accepted.
- [x] Define how frame-derived image/baseline/OCR/pixel assets map into existing `AutomationWorkflowDraftVisualAssets`. Evidence: `SemanticRecordingReviewDraftPatchBuilder` emits visual region/image/baseline upsert patch ops before condition mutation, and `SemanticRecordingReviewProjectionTests` applies OCR/image patches to real draft documents.
- [x] Define safe relative artifact refs shared with Workflow run evidence.
- [x] Define app-edge presenter integration for semantic recording preview refs. Evidence: `SemanticRecordingReviewPresenter` loads full sidecar-aware bundles, resolves safe artifact statuses, and centralizes Open/Reveal.

## Phase 1: Video And Keyframes

- [x] Pure semantic capture session can build a validating bundle from fake movie/frame/index clients and `RecordedEvent` inputs.
- [x] Semantic recording preflight evaluator distinguishes blocking and degraded capabilities for Input Monitoring, Screen Recording and Accessibility. Evidence: `SemanticRecordingPreflightTests` covers authorized, missing Screen Recording, missing Accessibility, keyframe-only and missing Input Monitoring paths.
- [x] Semantic recording preflight presentation projects ready/blocked/degraded results into app-shell status, issue rows and action intents without SwiftUI side effects. Evidence: `SemanticRecordingPreflightPresentationTests`.
- [x] App shell surfaces first-pass semantic recording preflight/degraded-mode guidance before starting semantic capture. Evidence: `MenuBarController` evaluates live preflight before countdown/start when `semanticRecordingEnabled` is on, stores the pure presentation on `AppState`, blocks missing required permissions, opens Settings guidance for blocked starts before countdown, and allows degraded capture with a status message.
- [x] Full SwiftUI preflight panel provides issue rows, degraded-mode copy and explicit Open Settings actions. Evidence: `SettingsPanel` renders `SemanticRecordingPreflightPresentation` status, issue rows, re-check action and permission-specific Open Settings buttons through `MenuBarController`.
- [x] Debug live-smoke runs can preserve preflight/bundle evidence metadata in a sidecar. Evidence: `SemanticRecordingDebugSmokeEvidenceSidecar` renders command plan, capture target, permission snapshot, app-facing `SemanticRecordingPreflightPresentation` guidance, bundle counts, persisted reload status, persisted counts, memory-vs-persisted count match/mismatch details, loaded/missing/failed sidecar diagnostics, redaction indexes, synthetic redaction rehearsal fields and review notes, `semantic-recording debug-smoke --evidence-sidecar <path>` writes it for blocked/preflight/finished runs, JSON output exposes `commandPlan`, `preflightPresentation` plus the same structured `persistedBundleCountCheck` status/mismatches and plain output prints preflight guidance plus `persisted bundle count match`, optional `--synthetic-redaction` / `--synthetic-redaction-reason <redacting-reason>` records an explicitly synthetic suppression request for safe-window renderer rehearsal, `SemanticRecordingDebugSmokeEvidenceTests` covers finished/blocked sidecars, command plan round trip/rendering, preflight guidance rendering, persisted reload evidence, structured count check Codable round trip, count mismatch rendering and deterministic synthetic suppression construction, and a local blocked-preflight CLI run wrote a non-empty sidecar while returning `evidenceSidecarPath` in JSON. This is a capture helper, not live product evidence by itself.
- [x] Ordinary Recorder has an experimental semantic capture bridge behind a non-default feature flag. Evidence: `SemanticRecorderBridge` and `Recorder.semanticRecordingStatus` start/record/finish/cancel wiring through `LiveSemanticRecordingSession`.
- [x] Semantic capture cancel/failure cleanup does not attach partial evidence to the saved macro. Evidence: `SemanticRecordingCaptureSession.cancel` / `SemanticRecordingLifecycle.cancel` stop movie capture without writing a stop keyframe, `LiveSemanticRecordingSession` removes temporary bundle directories on cancel/start failure, and `SemanticRecorderBridge` cancels the active session before reporting start/record/finish failure; `SemanticRecordingCaptureTests|SemanticRecordingLifecycleTests` cover cancel ordering.
- [x] Saved macro metadata can link to the semantic recording bundle produced by the experimental bridge. Evidence: `SavedMacro.semanticRecording`, `MacroSemanticRecordingReference`, `MenuBarController` attachment logic, and `SavedMacroPreviewCacheTests`.
- [x] Ordinary Recorder passes frontmost window capture identity to the experimental semantic bridge. Evidence: `SemanticRecordingCaptureTargetMapper`, `Recorder.startRecording(semanticCaptureTarget:)`, and `SemanticRecordingCaptureTests` cover target-window mapping and display fallback.
- [x] Ordinary Recorder feeds first-pass live suppression contexts into the experimental semantic bridge. Evidence: `LiveSemanticRecordingSuppressionContext` builds app-edge contexts from current capture target, AX focused secure/password-field hints, window-title domain candidates and UserDefaults exclusion rules; `Recorder` sends changed contexts through `SemanticRecorderBridge` to `LiveSemanticRecordingSession.addSuppressions(for:)`; `RecordingEngineDiagnostic.eventTapDisabledByUserInput` maps event-tap disabled-by-user-input into Secure Input suppression context; `SemanticRecordingSuppressionTests` cover the pure producer rules and `RecordingEngineClientTests` cover fake diagnostic streams.
- [x] First-pass user-facing exclusion settings map into the live suppression rule keys. Evidence: `SemanticRecordingSuppressionSettings` normalizes app bundle IDs, window-title fragments, domains and max artifact bytes, `AppState` persists those fields to UserDefaults, `SettingsPanel` exposes Privacy exclusions under the experimental visual-evidence setting, and `SemanticRecordingSuppressionTests` covers parsing/de-duplication/rule handoff.
- [x] Sensitive/excluded contexts suppress the experimental semantic visual capture bridge before attaching bundle evidence. Evidence: `SemanticRecordingSuppressionProducer.captureSuppressionDecision(for:)` treats Secure Input, focused password fields, excluded apps/windows/domains and private regions as capture-suppressing reasons, while retention-only oversized artifacts remain record-only; `Recorder` checks that decision before semantic bridge start and during event/diagnostic flush, calls `SemanticRecorderBridge.suppress` to cancel/remove the semantic bundle, reports `SemanticRecorderBridgeStatus.suppressed`, and keeps ordinary macro recording running; `SemanticRecordingSuppressionTests` cover the pure decision.
- [x] Suppression-matched AI-safe text evidence is redacted inside semantic bundles. Evidence: `RecordingSuppressionReason.redactsSemanticEvidence`, `SemanticRecordingCaptureSession` redacts matched `RecordingSemanticEvent` titles/summaries/evidence refs plus matched OCR/AX/window observation text/confidence/artifact refs when suppression records identify an event, frame, exact time or time range; `SemanticRecordingCaptureTests.suppressionRedactsFutureAISafeTextAndVisualObservationText` covers a future suppression window.
- [x] Suppression-matched frame/video redaction can be planned deterministically. Evidence: `SemanticRecordingRedactionPlanner` maps redacting suppression records to frame masks and video time ranges, prefers OCR/AX/window observation bounds, falls back to full-frame masks when only frame dimensions exist, generates stable `redacted/frames/<frame-id>.png` refs, and ignores record-only oversized artifact suppression; `SemanticRecordingRedactionTests` covers localized masks, full-frame fallback, video ranges and ignored record-only reasons. Frame/video writing is covered by the app-edge items below.
- [x] Suppression-matched frame/video redaction has app-edge writing hooks and live finish application. Evidence: `SemanticRecordingFrameRedactionRenderer` decodes source frame PNGs, clips masks and writes planned `redacted/frames/<frame-id>.png` artifacts; `SemanticRecordingVideoRedactionRenderer` exports full-frame redacted `.mov` ranges; `RecordingBundleStore.applyRedactionPlan` / `applyRedactions` writes redacted frame/video indexes; and `LiveSemanticRecordingSession.finish` applies non-empty redaction plans after bundle write before reporting success. Swift 6 build compile-verifies the app-edge file IO path. Live product evidence remains open.
- [x] Review and CLI prefer redacted frame refs when a redacted frame sidecar exists. Evidence: `SemanticRecordingBundle.redactedFrames`, `RecordingBundleStore.loadBundle` reads `redacted/frames/index.json`, `SemanticRecordingReviewProjection` exposes redacted-first `imageRefPath` while preserving `sourceImageRefPath`, S4 frame payloads expose `effectiveImageRef` / `redactedImageRef`, and targeted tests cover bundle sidecar merge, Review projection preference and CLI effective refs. Live product evidence remains open.
- [x] Persisted semantic bundles can be reloaded from manifest plus sidecars. Evidence: `SemanticRecordingBundleSidecars` / `SemanticRecordingBundle.applyingSidecars` define tested merge semantics, `SemanticRecordingBundleDirectoryIdentity` covers canonical UUID directory names and mismatch rejection, strict `RecordingBundleStore.loadBundle` overlays `video/segments.json`, `frames/index.jsonl`, `timeline.jsonl`, `events.jsonl`, `ocr/observations.jsonl`, `suppressed.jsonl` and `redacted/frames/index.json`, explicit `loadBundleTolerant` returns `SemanticRecordingBundleLoadResult` with loaded/missing/failed sidecar diagnostics and manifest fallback for corrupt optional sidecars, `listBundleCatalog` exposes only matching bundle ids, `SemanticRecordingReviewPresenter` uses the full loader, S2 retention cleanup preview uses the tolerant path, and S4 explicit stored-bundle read-only CLI consumes the loader/catalog. Product-ready default/live catalog remains open until authorized live evidence and suggestion synthesis are ready.
- [x] Persisted semantic bundles can be audited for Review/AI readiness beyond schema validity. Evidence: `SemanticRecordingBundleReadiness` checks expected video/keyframes/timeline/AI-safe events, optional OCR/window/AX observations, frame-to-video alignment and rendered redaction sidecars for sensitive suppressions; `SemanticRecordingBundleReadinessTests` covers ready redacted fixture, missing redaction sidecars, Codable issue round trip, keyframes-only policy and frame-to-video alignment; `semantic-recording debug-smoke` finished payloads reload the just-written bundle through `loadBundleTolerant(from:)`, then report persisted reload counts, structured `persistedBundleCountCheck` match/mismatch details, sidecar diagnostics, readiness policy, status, issue counts, issue details and follow-up actions, with `--require-ocr` / `--require-window-or-ax` available for stricter authorized live evidence review. Live product evidence remains open.
- [ ] Record target-window `.mov` during macro recording through `SCRecordingOutput`.
- [ ] Record target-window keyframes during macro recording.
- [ ] Persist video segment metadata with start/end time, capture target and codec/file info.
- [ ] Persist semantic recording bundle files and artifacts through an app-edge bundle store.
- [ ] Provide keyframe-only light mode only after default video path is safe and reviewable.
- [ ] Persist frame index with event time alignment.
- [x] S3 fixture Review projection opens `SemanticRecordingFixture.checkoutBundle()`, exposes frame strip rows, event before/after frame ids, selected-frame overlays, source/runtime comparison rows and review-only suggestion rows. Evidence: `SemanticRecordingReviewProjectionTests`.
- [ ] Show recorded video/keyframes in Macro Review. Keyframe image display has a first pass through `SemanticRecordingReviewPresenter`; recorded video playback remains open.
- [x] Click an event row and jump to before/after frame. Evidence: `SemanticRecordingReviewFixtureView` timeline rows select events and Before/After chips select the corresponding frame ids.
- [x] Unit-test timestamp/frame/event alignment with fake clocks and fixtures. Evidence: `SemanticRecordingCaptureTests` covers movie segment + start/event/stop keyframes, keyframe-only mode, OCR observation attachment and lifecycle ordering.

First-pass note: `SemanticRecordingCaptureSession` can assemble fake-client video/keyframe bundles, redact suppression-matched AI-safe text/OCR evidence, `SemanticRecordingPlayableSanitizationPlanner` can explain and explicitly sanitize readable `RecordedEvent` text fields for matched suppressions, `SemanticRecordingRedactionPlanner` can plan suppression-matched frame masks and video ranges, `SemanticRecordingFrameRedactionRenderer` can write redacted frame PNGs through `RecordingBundleStore`, `SemanticRecordingVideoRedactionRenderer` can export redacted `.mov` range overlays through the same store hook, `LiveSemanticRecordingSession.finish` applies non-empty redaction plans after bundle write, Review/CLI frame payloads can prefer redacted frame refs, `SemanticRecordingBundleReadiness` can audit persisted bundle readiness beyond schema validity, `SemanticRecordingPreflight` can classify blocking/degraded readiness, `semantic-recording debug-smoke --evidence-sidecar` can preserve smoke/preflight evidence metadata, optional `--synthetic-redaction` can rehearse redaction sidecar generation on safe content, Settings can render preflight guidance, retention settings and manual cleanup confirmation, `SemanticRecordingRetentionPresenter` / `SemanticRecordingRetentionCleanupPresenter` can project retention states and cleanup previews without file IO, Recorder can feed first-pass suppression contexts, suppress semantic capture on sensitive contexts, receive Secure Input diagnostics, and automatically withhold playback-preserving readable macro text metadata on save/export when semantic suppressions match. App-edge `ScreenCaptureKitMovieRecorder` / `ScreenCaptureKitFrameSource` / `RecordingBundleStore` / `LiveSemanticRecordingPreflight` compile. Live macro recording product evidence, recording-start guidance product evidence, redacted frame/video product evidence, reviewed text-anchor mutation and live cleanup product evidence remain open.

## Phase 2: Visual Index

- [ ] Run OCR on selected/key frames through app-edge Vision adapter.
- [ ] Persist OCR observations with frame IDs and bounding boxes.
- [ ] Persist window/surface metadata snapshots.
- [ ] Support pixel sampling from recorded frames.
- [ ] Support image template/baseline extraction from recorded frames.
- [ ] Store source frame ID, surface ID, crop bounds, search region and threshold for every extracted visual asset.
- [x] Preview template/baseline refs in UI with thumbnail/diff/source-frame evidence. Evidence: `template-baseline-preview-refs.png`, `semantic-review-timeline.png`, `semantic-review-stored-bundle.png` and `semantic-review-materialized-actions.png` show source/runtime/diff refs and package-local materialized asset evidence in fixture/stored Review surfaces; live installed-app evidence remains open.
- [x] Add fixture tests for path safety, coordinate transforms, OCR observation parsing and asset references. Evidence: `SemanticRecordingBundleTests`, `AutomationContractTests`, `SemanticRecordingQueryTests`, `SemanticRecordingCLITests` and `SemanticRecordingReviewProjectionTests`; targeted S4 query/CLI tests passed on 2026-07-07.

First-pass note: `VisionRecordingIndexer` can turn stored frame PNGs into `RecordingVisualObservation.ocrText` payloads, and `SemanticRecordingCaptureTests` proves observations can attach to bundle frames. User-facing OCR indexing and persisted live bundle evidence are still open.

## Phase 3: Editing UX

- [ ] User can create OCR wait from recorded frame selection.
- [ ] User can create image appeared/disappeared condition from recorded frame crop.
- [ ] User can create region changed baseline from recorded frame region.
- [x] Frame-to-condition candidates generate review-only draft patches without mutating the original playable macro. Evidence: `SemanticRecordingReviewDraftPatchBuilder`, `upsertVisualRegion` / `upsertVisualImage` / `upsertVisualBaseline`, and `SemanticRecordingReviewProjectionTests`.
- [x] Review-generated frame-to-condition patches can open Draft Preview and use the existing confirmed import path without mutating the original workflow before confirmation. Evidence: `SemanticRecordingReviewPresenter.previewState`, `SemanticRecordingReviewFixtureView`, `AutomationMainContentView.importWorkflowFromDraftPreview` handoff, and Swift 6 build.
- [x] Pixel condition candidates can use a reviewed color picker instead of requiring metadata-provided `colorHex`. Evidence: `SemanticRecordingReviewFixtureView` uses `AutomationVisualColorPickerView`, and `SemanticRecordingReviewProjectionTests.pixelConditionCandidateCanUseUserPickedColorWithoutMetadata` applies a `pixelMatched` draft patch with user-picked color.
- [ ] User can replace one fragile coordinate click with visual locator suggestion.
- [x] Fixture Review UI exposes frame-to-condition candidates without mutating the playable macro. Evidence: `SemanticRecordingReviewProjectionTests` and `workflow product-evidence snapshot semantic-review-timeline`.
- [x] UI shows evidence-backed explanation for each accepted suggestion. Evidence: `semantic-review-timeline.png` / `.md` and `semantic-review-materialized-actions.png` / `.md` show accepted decision explanation, evidence refs and `review.previewDraft` / `review.importDraft` action rows; live installed-app suggestion review remains future evidence.
- [x] User can compare recording baseline, runtime last sample, and watched-region crop in one drill-in. Evidence: `template-baseline-preview-refs.png` and `semantic-review-timeline.png` render source/runtime/diff comparison, while `visual-diagnostics-drill-in.png` and live visual diagnostics evidence prove watched-region/last-sample artifact preview in Workflow Run Detail; live semantic recording source/runtime clip remains S3 follow-up.
- [x] User can reject an AI/automatic suggestion without mutating the original playable macro. Evidence: `semantic-review-timeline.png` / `.md`, `SemanticRecordingReviewProjectionTests` and S4 suggestion `review.rejectSuggestion` / `review.clearDecision` actions keep rejection local to Review and do not mutate workflow or macro storage.

## Phase 4: CLI

- [x] S4 CLI/AI workstream documents fixture-first MVP, CLI-first boundary, MCP deferral and App Knowledge later. Evidence: `docs/semantic-recording-ai/workstreams/s4-cli-ai-app-knowledge.md`.
- [x] Fixture `recording show checkout-demo --fixture checkout --json` returns `sparkle.cli.result.v1`, fixture-mode warning, summary counts and safe artifact refs. Evidence: `SemanticRecordingCLITests`.
- [x] Fixture `recording frames`, `recording frame show` and `recording events-near` return frame IDs, event IDs, timing, surface IDs and safe artifact refs without image bytes. Evidence: `SemanticRecordingCLITests` and transcript in `workstreams/s4-cli-ai-app-knowledge.md`.
- [x] Explicit stored-bundle `recording list/show --json` first pass uses `--recordings-root <path>` / `--bundle-path <dir>` and S2 `RecordingBundleStore` without choosing a default live root. Evidence: `SemanticRecordingCLITests`, S4 transcript, executable build.
- [x] Fixture and explicit stored-bundle `recording explain --json` first pass returns read-only semantic key points, visual evidence summaries, evidence notes, safe artifact refs and Review/Draft Preview mutation policy without reading image bytes or invoking Vision. Evidence: `SemanticRecordingCLITests`, executable CLI smoke.
- [ ] Product-ready default/live `recording list/show/explain --json`
- [x] Explicit stored-bundle `recording frames/frame show/events-near --json` first pass uses loaded bundles and safe refs without image bytes. Evidence: `SemanticRecordingCLITests`, executable build.
- [x] Fixture `recording ocr search --json` returns deterministic OCR observation ids, text, bounds, confidence, query result ids, `availability: deterministicFixture` and safe artifact refs without invoking Vision. Evidence: `SemanticRecordingQueryTests`, `SemanticRecordingCLITests`.
- [x] Explicit stored-bundle `recording ocr search --json` first pass filters persisted OCR observations from loaded bundles without invoking Vision and reports `availability: persistedBundle`. Evidence: `SemanticRecordingCLITests`, executable build.
- [ ] Product-ready default/live `recording ocr search --json`
- [x] Fixture and explicit stored-bundle `recording visual search --json` first pass filters persisted visual observations by kind/label/text and returns observation ids, frame ids, bounds, confidence/score, labels and safe artifact refs without reading image bytes or invoking Vision. Evidence: `SemanticRecordingQueryTests`, `SemanticRecordingCLITests`, S4 transcript, executable build.
- [ ] Product-ready image-byte visual similarity search
- [x] Explicit-source `recording asset extract/baseline --json` first pass crops caller-selected frame regions into `--output-root/assets/images` or `--output-root/assets/baselines`, returns SHA-256 and `AutomationWorkflowDraftVisualAssets`-compatible refs, and refuses missing source artifacts / unsafe paths. Evidence: `SemanticRecordingCLITests`, executable CLI smoke.
- [x] Fixture `recording suggest waits/conditions --json` returns deterministic review-only suggestions with query `allowedKinds`, `availability: deterministicFixture`, evidence refs, confidence, risk, fallback and mutation policy. Evidence: `SemanticRecordingQueryTests`, `SemanticRecordingCLITests`.
- [ ] Live/stored-bundle `recording suggest waits/locators/conditions/cleanup --json` with real suggestion synthesis. Current first pass intentionally returns `availability: unavailable`, `unavailableReason` and `suggestionsUnavailable` warning for stored/live sources.
- [x] Fixture/review-only `workflow draft from-recording --json` first pass emits `sparkle.workflow.draft.v1`, cites recording suggestion/frame/event/observation evidence, writes `--out` when requested, and the generated draft passes existing validate/simulate CLI gates without import or execution. Evidence: `SemanticRecordingCLITests`, executable CLI smoke.
- [ ] Product-ready stored/live `workflow draft from-recording --json` with real suggestion synthesis, missing/deleted artifact status and Review/Draft Preview alignment.
- [x] Fixture recording CLI results use stable `sparkle.cli.result.v1` envelopes.
- [x] Fixture recording CLI returns evidence refs by default and does not export image or video bytes.

## Phase 5: AI Draft Integration

- [x] Fixture/review-only AI/CLI path can generate a `sparkle.workflow.draft.v1` from one recording. Evidence: `workflow draft from-recording checkout-demo --fixture checkout --out /tmp/s4-maintenance-draft.json --json` returned `ok: true` and wrote a draft from cited OCR/frame evidence; product-ready stored/live draft synthesis remains open in Phase 4.
- [ ] Draft Preview shows evidence references from recording frames in live product evidence. First-pass handoff exists from Review-generated patches, but live recording evidence remains open.
- [x] Validate/simulate/dry-run/import works without AI writing internal workflow JSON. Evidence: `semantic-recording-cli-low-token-transcript.md` records validate `isValid: true`, simulate `isSimulatable: true` and import dry-run `isImportable: true` for the generated fixture draft.
- [x] User can reject or patch AI suggestions. Evidence: S3 Review action semantics expose `review.rejectSuggestion`, `review.clearDecision`, review-only patch saving and Draft Preview handoff; S4 suggestion JSON mirrors those actions without direct workflow mutation.

## Phase 6: App Knowledge

- [ ] Group recordings/macros by app bundle ID and surface family.
- [ ] Build app knowledge summary from existing recordings.
- [ ] CLI can answer which existing macros/anchors may satisfy a natural-language goal.
- [ ] AI can compose a draft from existing macros without requiring a new recording when evidence is sufficient.
- [ ] UI explains reused recordings and missing evidence.

## Product Evidence

- [x] Fixture recording review screenshot with frame, event row and OCR overlay. Evidence: `docs/workflow-page-productization/product-evidence/semantic-review-timeline.png` and `.md`.
- [ ] Recording review screenshot with video frame, event row and OCR overlay.
- [ ] Frame-to-condition creation clip.
- [ ] AI cleanup suggestion screenshot with evidence explanation.
- [x] CLI transcript showing low-token query flow over local visual index. Evidence: `docs/workflow-page-productization/product-evidence/semantic-recording-cli-low-token-transcript.md`.
- [x] Draft-from-recording preview screenshot. Evidence: `docs/workflow-page-productization/product-evidence/semantic-review-draft-preview.png` and `.md` show the recording-derived fixture Review patch in the normal Draft Preview sheet with visual asset provenance; live Draft Preview evidence remains open above.

## Safety Gates

- [x] Secure Input and password fields suppress visual/text evidence. Evidence: `SemanticRecordingSuppressionProducer`, `LiveSemanticRecordingSuppressionContext`, `Recorder` -> `SemanticRecorderBridge.suppress`, `SemanticRecordingCaptureSession` AI-safe text/OCR redaction, playable macro sanitization, frame/video redaction planners/renderers and suppression tests cover first-pass suppression/redaction; live sensitive-context product evidence remains separate.
- [x] Secure Input, password fields, excluded targets and private regions suppress semantic visual bundle capture in the experimental path. Evidence: capture-level suppression decision and `Recorder` -> `SemanticRecorderBridge.suppress` wiring.
- [x] Suppression records redact matched AI-safe semantic text and OCR observation text. Evidence: core redaction in `SemanticRecordingCaptureSession` and playback-preserving playable macro sanitization on save/export through `SemanticRecordingPlayableSanitizationPlanner`, `MacroPlayableSanitizationSummary`, `MacroLibrary.applyPlayableSanitization` and `MenuBarController` export helpers. Reviewed text-anchor mutation remains future work.
- [x] Suppression records produce deterministic local playable-text, frame and video redaction plans. Evidence: `SemanticRecordingPlayableSanitizationPlanner` maps sensitive/excluded suppression to readable `RecordedEvent` fields, and `SemanticRecordingRedactionPlanner` maps sensitive/excluded suppression to frame masks and video ranges without touching files or live APIs; frame PNG and `.mov` store hooks exist separately.
- [x] Redacted frame PNGs and full-frame redacted `.mov` range exports can be produced locally from the redaction plan and are applied during live finish when a non-empty plan exists. Evidence: `SemanticRecordingFrameRedactionRenderer`, `SemanticRecordingVideoRedactionRenderer`, `RecordingBundleStore.applyRedactionPlan` and `LiveSemanticRecordingSession.finish`; live product evidence remains open.
- [x] Excluded apps/windows/domains produce suppression records. Evidence: `SemanticRecordingSuppressionSettings` normalization/persistence, `LiveSemanticRecordingSuppressionContext` app/window/domain rules, `SemanticRecordingSuppressionProducer` record generation and `SemanticRecordingSuppressionTests`.
- [x] Full video is never sent to AI by default. Evidence: S4 CLI envelopes and `semantic-recording-cli-low-token-transcript.md` return compact summaries, ids and safe artifact refs only; no image/video bytes or whole-video payload are emitted by default.
- [x] User can delete semantic bundle artifacts. Evidence: `SemanticRecordingRetentionPlanner`, `SemanticRecordingRetentionPresenter`, `SemanticRecordingRetentionCleanupPresenter`, `RecordingBundleStore.applyRetentionPlan`, Settings cleanup UI wiring and scheduled cleanup planner; live cleanup product evidence remains open in Phase 0.
- [x] App-edge presenters handle reveal/open; SwiftUI does not construct raw paths. Evidence: semantic Review artifact actions go through `SemanticRecordingReviewPresenter`.
- [x] AI suggestions cite frame/event/evidence IDs; suggestions without evidence stay marked low confidence. Evidence: fixture suggestions cite frame/event/observation/artifact refs; `SemanticRecordingCLISuggestionSummary` caps no-evidence suggestions at `0.49` confidence and appends a missing-evidence risk, covered by `SemanticRecordingCLITests.recordingSuggestionWithoutEvidenceStaysLowConfidence`.
- [x] Semantic recording can be disabled without breaking ordinary macro recording/playback. Evidence: `semanticRecordingEnabled` defaults off, ordinary `Recorder.startRecording` accepts a per-run semantic flag, and disabled sessions skip `SemanticRecorderBridge`.
